---
name: machine-setup
description: Per-machine environment setup and verification for Roam. Consult when a session starts on a fresh or unfamiliar machine, when git identity or the machine label looks wrong, when hooks or tools misbehave on one computer only, when the founder says they're on a new/other PC, or when the founder says "apply the vault lens" (or any close variant like "set up the vault lens" / "install the Obsidian lens") — that phrase runs the §Vault lens procedure.
---
Read docs/skills/machine-setup.md and follow it exactly.
