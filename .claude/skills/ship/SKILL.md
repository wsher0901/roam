---
name: ship
description: Run yourself the moment a task's payload is complete and the CI mirror is green, BEFORE any "done" claim in conversation; never declare done without it.
allowed-tools: Bash(gh pr merge --squash --delete-branch:*)
---
Read docs/skills/ship.md and follow it exactly.
