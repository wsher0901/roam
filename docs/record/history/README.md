---
type: reference
title: History legend
status: living
---
# History — the four doors

Every shipped story lives behind exactly one door. Pick the column
by IS vs DOES, the row by Roam-facing vs builder-facing; ties →
workshop/definition.

- product/definition — what Roam IS: identity, roadmap shape,
  engine rules, fact and traveler vocabularies.
- product/build — what Roam DOES: scaffold, CI, deploys, engine and
  app code.
- workshop/definition — what the WORKSHOP is: laws, policies,
  setup, knowledge system, conventions.
- workshop/mechanism — what the workshop DOES: rituals, hooks,
  lanes and fleet, board machinery, seat tooling.

The weld (ship §7) assigns the quadrant at move time. When a
quadrant grows unwieldy, it gains version sub-buckets by decision —
rolling wave, never speculatively.

## The ledger
Every weld prepends one line — the full chronology across all four
doors, newest first. Frozen stories carry date only; entries from
D-035 on carry time.

- 2026-07-27 20:42 · atlas — the workshop gains its system spine (D-058): docs/ATLAS.md is born — six mermaid diagrams on one page, the task loop with the weld's four acts drawn inside it, the file flow, away-and-return closed into a circle (liftoff → cockpit → land's two modes → pickup's fleet resume), a lane's life with the canary and the wake-lock, the surfaces and who may write what, and the state surfaces the board renders — each composed FROM the living prose at HEAD, never from memory. The posture is the board's and it is the whole decision: IT RENDERS, ORIGINATES NOTHING — dated stamp, least-authoritative, the prose governs, and beneath every diagram a Boxes: weave line links each doctrine-carrying box to the section that owns it, so the checker and any currency audit read the page mechanically; in-diagram click-links REJECTED with both mechanical reasons on record (check-links strips fences; GitHub's sandboxed mermaid ignores click directives). HOME gained the one routing row the mandate allowed, its upsert rule doubling as the maintenance law — the bench that changes what a diagram depicts re-renders it in the same PR — and the catalog entry the cap excluded was surfaced at the gate and landed on the external review's explicit license. THE CRITIC'S ONE REAL CATCH was the sharpest kind for a page whose single law is accuracy: the ack-token box showed a non-canonical separator — the exact #191 failure class the canary contract exists to prevent — repaired to middots character-for-character; folded with it the fence rewording, the cockpit's seat vocabulary, the LOCAL spawn vehicle, the micro-PR carve-out finally depicted, and a mermaid cluster-edge reorder. External Web review PASS on 657040c: all 46 links fragment-verified on the reviewer's own resolver, diagram accuracy hand-checked against the owning prose, gates re-run independently; the visual render judged by the founder's own glance → workshop/definition · #231
- 2026-07-27 20:13 · record-shelf — the frozen record moves under one roof (D-057): docs/record/ is born and the three append-only surfaces move in as recorded renames — 155 files (138 edited + 17 exact), history, specs, and DECISIONS under one shelf while memory/ stays at root because in-flight is living; THE ENABLING RULING makes a link-path-segment repair inside a frozen file MAINTENANCE, not a content edit — append-only governs meaning, not bytes (precedent #163, #204) — and the review criterion was stated so a machine could check it: the record diff with link targets blanked is byte-identical line-for-line, the sole exception the bench's own spec. The sweep ran as ONE deterministic recompute — resolve every link from the file's old location, map moved targets into record/, recompute from the new location — 1,580 links across 158 files, living files gaining record/, frozen files gaining one ../, intra-record links preserved by construction; the external review qualified the premise, the qualification welded into the story: eight docs-root-relative intra-record links across six record files WERE broken by the move and repaired by the same recompute. Machinery repointed: check-ledger's walker root, ship's weld destination + staging command, the reviewer agent's two paths, the vault-seed filter -path:record. THE CRITIC'S ONE REAL CATCH: a broken DASHBOARD ledger link whose text nests a backticked bracket pair, invisible to the checker's text-blind link regex — so "0 broken" had been a statement about the checker, not the corpus; the link repaired AND check-links.mjs hardened to parse one nested bracket level, 13 previously-skipped links entering the count (3,106/0). The living door doc record/specs/README.md's dead naming path was HELD at the gate — the zero-prose-in-record/ mandate outranking the repair — and fixed on the founder's explicit ruling in the review paste; external Web review PASS on 999c061, prose invariance and all 155 rename pairs verified on the reviewer's own clone with the gates re-run independently → workshop/definition · #229
- 2026-07-24 17:21 · team-aware-leaving — no ritual lets a live agent team outlive the sitting that made it (D-056): D-055 had made single-sitting-only law and left it enforced by nothing — handoff parked lanes, land fenced them, neither mentioned a team — and a rule documented in two surfaces and acted on by none breaks the first time a team is running when the founder says "done for today". THE DECISION IS AN INVERSION of the candidate clause that opened the bench. "A live team is finished or shut down, never parked" governs the PROCESSES, which are not what is at risk: teammates die cheaply and respawn cheaply, while what dies irrecoverably is what they FOUND, because a team's output lives in transcripts that vanish with the session and git keeps everything else. So the duty became CAPTURE, THEN KILL — handoff §1.2, before any park: write the findings into the record first (this session's OWN bench memory, never a lane's diary, else an IDEAS line riding §5's micro-PR), THEN shut down by name, THEN report what each returned, with shutdown's not-instant honesty and a refusing-or-hanging teammate named plainly in the close report on the road out. land states the cockpit case once and routes to handoff with zero duplicated procedure — a copy there would be a procedure that can never run; pickup gains a one-paragraph ghost-teammate recovery note; TEMPLATE's ⏸ block gains a conditional TEAM FINDINGS field, the one thing git cannot re-derive. Three rejections with reasons (auto-shutdown without reporting · persisting a team across sittings · duplicating the procedure into land). THE CRITIC FOUND THREE ALREADY-SHIPPED HOLES, each the exact silent loss the bench exists to prevent: refusals routed to handoff §6 which LIFTOFF SKIPS, so the report had nowhere to land on that road (the disclosed liftoff ripple, judged REQUIRED and not drift at the external review); "the open bench's memory" not saying WHOSE, which licensed writing a findings block into a lane's diary and would have broken the very park protocol this entry's title claims to uphold; and — on the Opus pass, which traced every road out of a sitting — the IDEAS fallback having NO COMMITTING VEHICLE, so in the precise no-bench case it existed for the findings would have been lost as completely as if nothing had been captured. It also caught a logic error in the bench's own justification: a cockpit "cannot" lead a team was founded on one-team-per-session, which does not imply it, so the basis was rewritten as POLICY (D-055's ground-seat boundary) in both homes — D-056 correcting D-055's mechanism-based reasoning FORWARD while leaving that entry unedited under the append-only law. Two corrections the founder accepted as his own ride the record: the mandate's premise about the critic's model (definitions are cut at session startup) and the inversion itself. One road stays uncovered and filed rather than papered over — the superseded-seat self-close, an exit no ritual runs; external Web review PASS on `fc4dcf7` with the gates re-run independently → workshop/mechanism · #225
- 2026-07-24 16:44 · agent-teams — where agent teams fit this workshop and where they must not (D-055): the experimental flag was already ON in `.claude/settings.json` with nothing reaching for it, so the question was never whether teams get used but whether their first use would be governed. THE BOUNDARY, filed in HOME beside the Command card: teams are for EXPLORATION AND REVIEW at a ground seat and LANES REMAIN THE AUTHORSHIP MECHANISM — a team never authors a bench, never merges, never runs a ritual — with the reasons stated rather than asserted (lanes are cloud-isolated, PR-gated, canary-claimed and wake-locked; teams are local, in-session, share ONE working tree, and have no PR isolation; and bench authoring is sequential same-file work, the exact shape the official guidance points away from teams). TWO RISKS BECOME LAW: single-sitting only — `/resume` and `/rewind` do not restore in-process teammates and a resumed lead may message teammates that no longer exist — written as an explicit CARVE-OUT to D-050's interrupt promise in BOTH places that promise is read, so a founder cannot meet the promise without its exception ("an interrupt is safe for your session and fatal to your teammates"); and a teammate-authored diff is SELF-AUTHORED, one LAWS clause beside no-solo-approval, because a teammate is a full session pushing as the founder and earns no independent review merely by being a second agent. THE REVIEWER'S MODEL settled by verification rather than guess: `/model` being interactive and unavailable from the harness, the `opus` alias was resolved MECHANICALLY from a headless run's own billing record (`canonicalModel: claude-opus-5`), recorded verbatim in the memory at `cb5648c` BEFORE `reviewer.md` was touched — an order the external review checked in the log rather than taking on trust — so the critic takes the ALIAS, not a deprecation-brittle pin. The founder-side config is recorded so either seat sets it up identically (Default teammate model → leader's model; effort inherits; in-process only, split panes being unsupported in Windows Terminal; permissions inherited, so the deny rails cover the team). Three rejections with reasons (teams for bench authorship · teams in the cockpit, where one-team-per-session and no-nested-teams settle it by construction · loosening teammate permissions). THE CRITIC RAN TWICE and the second run is the story: the first reported itself `claude-sonnet-5` because this session's agent definition was cut at startup before the edit — so the new model reaches sessions started after the merge, not the session that makes the change — and the second, forced onto `claude-opus-5`, found a THIRD stale copy of the retired Sonnet-teammate rule plus the missing D-045/D-020 forward pointer; of three stale copies in total the bench found one and the critic found two. One gap deferred on purpose and filed to IDEAS rather than silently dropped: risk (a) is documented in two surfaces and enforced by no ritual — the founder's gate word widened it, since land must learn about live teams as well as handoff; external Web review PASS on `9b798b2` with the gates re-run independently → workshop/definition · #223
- 2026-07-24 15:20 · landing-skill — how a flight ends becomes a first-class ritual (D-054): land ships as ONE skill with TWO modes routed by the fleet the cockpit derives from origin — RETIRE, consolidating the duties charter rule 6 already carried, and PAUSE-AND-TRANSFER, answering the case that had none: a founder walking back to a desk while lanes still fly. Its core is THE FENCE, a commit on the lane's branch that makes the lane's next push non-fast-forward, so the wake-lock turns the rejection into a stop and THE RESUME POINT IS THE BRANCH TIP BY CONSTRUCTION — nothing must be recorded correctly for it to be right, and the only possible loss is an unpushed sliver that push-every-commit already bounds — with the honesty clause written beside it (wake-lock-mediated, not an interrupt: a lane mid-turn may finish its current step, so a landing report states last-observed state, never a guaranteed freeze). The board's fleet-at-ground table is the single material passed down; sessions are cattle and branches are the work (the archive list ordered AFTER the fences are pushed, old sessions never revived — resume births fresh workers on the fenced branches); pickup gains the FLEET RESUME ask with its three granular words and cap arithmetic derived at ask time, plus the redeploy mechanics in one home and a single pointer line from parallel-lanes; charter rule 6 reduces to a pure pointer; and the effort doctrine moves to Opus 5 · xhigh for ALL work, retiring the Opus 4.8 / Sonnet 5 reliance pair. Five alternatives REJECTED each with its reason (an instant lane freeze — lanes have no interrupt channel · auto-redeploy at pickup · pausing by session archive alone · reviving old lane sessions · two separate skills). The critic returned PASS-WITH-FINDINGS and both were repaired pre-flip — the 🟡 one, TEMPLATE's `held` row documenting a single writer, judged by the external review to be REQUIRED by one-home rather than merely allowed, so the finding improved the mandate; two files beyond the mandate's list were touched and disclosed at the gate, both forced by the retroactivity law; external Web review PASS on `3e4d1ba` with the gates re-run independently → workshop/mechanism · #221
- 2026-07-24 01:33 · guardrail-audit — the harness found ALREADY AT BEST PRACTICE, and the proof welded to the record (D-053): the founder's maximum-depth read of the whole governing layer (LAWS · HOME · the SETUP charter · all nine stubs · all three hooks · the reviewer · settings · AGENTS) against Anthropic's guidance — the always-loaded file governs, verification loops multiply quality, sparse emphasis outperforms shouting — found marker density 1–7 per file, thin single-source stubs, mechanics living in hooks, and the constitution always-loaded via the CLAUDE.md import; the audit's only two real improvements shipped: LAWS §Workflow's CI-mirror sentence promoted to a labeled, ordered, COPY-RUNNABLE block (proven live — this bench's own gate ran the block verbatim as its mirror) with sync-first and Actions-green kept around it, and two stub trigger-surfaces sharpened one line each (liftoff: machine-going-dark, watch-from-phone belongs to go-remote · ship: payload complete + mirror green, BEFORE any "done" claim), the harness picking both up live mid-bench — the audit's premise demonstrating itself; five alternatives REJECTED each with its reason (a BRIEF.md/.claude-rules home · a marker-economy pass · duplicating HOME's routing table · a session-shape law line · settings/hook changes); the decide-anchor length filed to IDEAS as a future consideration; the critic's one finding — this hygiene bench's own bare D-053 mentions, exactly the class the audit exists to catch — repaired pre-flip; external Web review PASS on `bd2f6ef` with the gates re-run independently → workshop/definition · #218
- 2026-07-24 00:51 · response-doctrine — one standard for how every surface speaks to the founder, and the Web master finally telling the truth (D-052): the founder's goal verbatim — minimum reading time to decision, explicit steps over abstractions, purpose-shaped explanation — became five clauses in ONE home (HOME §Response doctrine: answer first · explicit steps with WHERE·WHAT·WHY on every prompt handed to the founder · purpose-shaped explanation last · live-derived state · glyph verdicts, every 🔴 naming the fix AND where) with every ritual and charter REFERENCING, never copying (verified by negative-grep on clause-(b)'s own phrasing); HOME §Command card collects the founder's platform commands one line each — the teleport line carrying D-049's relocation warning exactly where it will be read — with liftoff §7 and the charter's R2 pointing instead of re-listing; the cockpit's turn-end report became the FLEET TABLE shape (fleet → needs your word with the exact word and where to type it → options+rec → context last, one message carrying everything a decision needs); liftoff §7, handoff §6, and pickup §5 were REWRITTEN — not annotated — to the four-part template (WORKING ON · WHAT HAPPENED · SUPPLEMENTAL · PARALLEL/RECENT FLIGHTS with 🟢/⏸ per item and exact resume words), pickup's EXIT TABLE dissolving into the flights list, the ⏸ INTERRUPTED lead staying above everything and the pacing-law NEXT surviving; WEB-INSTRUCTIONS jumped v4 → v5 — the paste-block-loop language DIED (zero grep hits), the mandatory external-review role leads with a four-step procedure ending in the on-the-record word-paste, all five surfaces mapped, the doctrine restated compactly inside the box as a sanctioned standalone-paste exception, the master-copy pattern kept; ship §6's critic returned PASS with no findings, its one spot-check observation (two bare D-052 mentions) repaired pre-flip to hold the #204 weave standard; external Web review PASS on `5f4be89` with the gates re-run independently → workshop/definition · #216
- 2026-07-24 00:28 · self-seat-birth — the fragilest component is deleted, not maintained (D-051): liftoff's rung 1 fires `claude --cloud` BLIND — hidden window for the TTY, prompt as a file-read argument, NOTHING captured, exit status only — and the cockpit SEATS ITSELF on the board as its first act by its D-049 env-derived self-URL, the mechanism successor-duty already used; the console-attach capture existed solely to hand the tower a session URL and was the component most likely to break SILENTLY on a platform UI change (the same flight that proved the launcher worked also proved platform behavior shifts — the title-line disproof), so it was retired dated rather than maintained, honest that it never failed; the board weld still precedes the fire (board-governs untouched), the Sessions row born "cockpit · fired <time> · self-seat pending", liftoff's close line reporting rung + exit code and NO URL — the greeting push and the self-seat repaint carry it; the trade named where both actors read it: a dead-at-birth session sends no greeting, and the MISSING push IS the signal (/tasks · the list-native claude.ai/code list · retry), the pending row going stale honestly over a scraped URL that may be wrong; the charter's stray-collision case sharpened by the critic at the gate — a fresh stray closes with a short factual note naming the live cockpit, never R4b's landed-superseded script, which presumes a flight the stray never had (APPROVED with the founder's merge word); two one-sentence note extensions retire attach-and-read from the records that had called it standing; THE LIVE TEST deliberately deferred to the first real flight, the founder's word on the record; external Web review PASS on `342e344` with the gates re-run independently → workshop/mechanism · #213
- 2026-07-23 23:38 · session-lifecycle — what a session's start, pause, and close leave behind, made deliberate (D-050, the founder's own gate fold answering the critic's routing question): CLOSED ≠ DEAD — the close-lock's wall softens from mechanical block (the prompt hook's exit 2) to injected read-only doctrine (exit 0, stdout-as-context), one doctrine in four homes (liftoff §7 · handoff §6 · HOME ×2 + a Terms line) and in the wall itself, full removal REJECTED because the two-writers risk the lock was built for is real, R4b's tombstone and the seat-stamp law byte-strict (SETUP zero-diff); the hook was the one file beyond the mandate's list — doctrine-only softening would have shipped an instruction that cannot operate, the #204 defect class — ruled the #197 gap-in-the-mandate pattern, both paths run live pre-commit, approved at the gate; INTERRUPT CAPTURE standing format — the ⏸ block (mandate · plan · done · exact next step · why paused), the `interrupted` state row, pickup LEADING with "⏸ INTERRUPTED — continue?", the capture duty scoped by the park protocol to tower-owned memories (a lane's diary keeps its one-line park stamp), and the validator needing NOTHING (verified against its logic before writing — TEMPLATE excluded, headings an ordered subset); the `claude/*` RESIDUE SWEEP graduates from ad-hoc founder orders to standing pickup hygiene — zero-unique-work per head, HOLD on live-session doubt, delete + the 60s prune tripwire that knows workspace branches are born lazily; IDEAS becomes an INBOX, NOT AN ARCHIVE — the compaction rule in its header and the harvest step, and the FIRST compaction run assert-first (420→266 lines, 11 explicitly-closed entries deleted and enumerated with their closing artifacts, 24 kept, both stay-clauses exercised on day one, the asserts catching a prettier-reflow near-miss before anything was touched); the chronicle layer and self-seat birth queued in IDEAS deduped; rung 3 reads "the summon engine, fired locally"; external Web review PASS on `edc0c9a` plus the pure-append D-050 head `1ffebdf`, merged on the founder's word once the delta was confirmed DECISIONS-only and green → workshop/mechanism · #211
- 2026-07-23 22:38 · gh-second-path — the cockpit gains a second API path and a connector flap stops costing command (D-049): the official docs contradicted two shipped certainties and a five-command cloud probe plus the founder-witnessed REST read proved them wrong on evidence — `gh` 2.45.0 installs from the allowlisted Ubuntu archive (the #193 exit-100 was the egress-blocked `cli.github.com` source, a wrong apt source read as a design wall) and authenticates AUTOMATICALLY through the GitHub proxy, `GH_TOKEN` the literal 14-char placeholder `proxy-injected` with real credentials substituted in transit; the path is REST-SHAPED — `gh api` works repo-scoped while GraphQL porcelain (`gh pr list`) 403s against a pinned operation set, the proxy's own message pointing to REST — so the probe flew NON-PASS on its own definition, the STOP held, and the gate reopened only on the founder's witnessed 4b (`gh api repos/wsher0901/roam/pulls` answering this very PR's number) with four folds: REST-shaped wording everywhere, gh probes as API reads never env echoes (classifiers treat GH_TOKEN as a secret), the toolset-bluff finding ridden first-class (a fresh session claimed "no gh" from its briefing without probing — probe-don't-assume extends to a session's own tools), and the three vehicle findings likewise (teleport RELOCATES execution to the local machine · the birth channel truncates on nested quotes, jq the specimen · a birth turn can no-op "No response requested", 1-of-4 executed); the ripple in one decide commit — SETUP's environment entry reversed, the API map two-pathed (only BOTH paths dead demote commander to author), the charter R2's automatic gh rung with CONNECTOR DOWN and self-rescue one rung deeper, self-ID from `CLAUDE_CODE_REMOTE_SESSION_ID` replacing console scraping for a session's OWN url, the Claude-Session trailer noted as the free audit link, liftoff's `/tasks` line — two pure-addition supersession notes (cockpit-resilience keeps its ladder, loses its sole-path premise; flight-hardening's gh correction corrected in turn), and the late-firing workspace-branch tripwire recorded (the platform creates `claude/*` branches lazily — five surfaced post-flight, zero unique work, held for archive-then-delete on the founder's order) → workshop/mechanism · #207
- 2026-07-23 21:29 · currency-audit — the docs currency audit: every live instruction that points at machinery that cannot fire, found and fixed, and every orphan hunted — the clerk sweep came back already-clean (#197 held), so the real catch was the NEXT generation of staleness: a verify-before-rely instruction whose verification already happened (the `[COCKPIT]` title line, disproven at the 2026-07-23 birth, dropped from liftoff §6 + SETUP with the answer recorded in place), a doctrine landed in one home but not its manual (#193's board-governs missing from HOME's liftoff portrait, plus three more HOME edges: FOUNDATION's writer drops the paste block, the skills list gains go-remote, §Delegation names route 1 as the recorded maiden winner), a shipped spec that reads as a recipe carrying two disproven shapes (cloud-born-cockpit, caught beyond the mandate's known set), and a per-machine obligation missing from the per-machine procedure (machine-setup gains the COCKPIT_ pair as a pointer to SETUP); four dated supersession notes as blockquotes under each spec title — annotation beat deletion throughout, zero deletions, zero record-body rewrites; orphans NONE by inbound-reference census, not eyeball; flown across two seats through a park — analysis at the work PC welded to the spec before any edit, implementation adopted at the home PC from origin alone; ship §6's critic passed all eleven changes and caught two weave breaches in the bench's own diary, repaired pre-flip; external Web review PASS on `614e0f8` with the gates re-run independently; the founder's gate answers on record — ENGINE's frontmatter folded to S3's first engine bench, all five PART C recommendations confirmed, the empty orphan list accepted, and the stale chore/liftoff-findings head deleted from origin on the gate order → workshop/mechanism · #204
- 2026-07-23 21:07 · env-clerk-scrub — .env.example carries zero CLERK_ cruft: the vestigial CLERK_ comment prose scrubbed from the "Routine fires" block so the file names only the live cockpit routine, the payload matching the founder's intent rather than their literal "two placeholder lines" (those assignment lines were already gone by #197); flown as the payload lane of a full liftoff run-through — bench-first birth, canary, cockpit airborne ack, one-file scrub — then carried by the cockpit through an independent non-author review that re-verified CI on the true merge tip af53dca4 (run #599 green) after the lane's record cited a run one commit behind, and welded on the founder's word → workshop/mechanism · #200
- 2026-07-23 12:02 · clerk-retirement — the repo stops pointing at a vehicle that cannot fire: the founder deleted the clerk routine on 07-22, ahead of the maiden-closeout trigger D-046 had staged the retirement to, which INVERTED the risk the staging protected against — the staging assumed paper would die before the vehicle, so its worst case was a live vehicle nobody used, where what actually happened is the reverse, leaving a fallback rung and a promised notification channel that could not fire, in exactly the places a seat reaches under failure; every LIVE instruction reaching for the clerk removed and every VERIFIED RECORD kept, on the governing distinction that an instruction tells a future seat to DO something (a false one is a trap) while a record says what WAS proved (deleting it destroys evidence) — liftoff's ladder bottoms out at the D-048 phone bootstrap with no rung beneath it and its cap arithmetic names `fire:cockpit` alone, `scripts/fire.mjs` takes `cockpit` only with the `process.exitCode` drain idiom and every honest exit code untouched (both probes RUN foreground with output captured: `clerk` exits 1 honestly, `cockpit` with fake credentials from a temp cwd is rejected 401 PRE-SPAWN — zero cap burn, and the honest 1 rather than the Windows 127 the idiom exists to prevent), `package.json` and `.env.example` following, and SETUP's cloud-clerk, charter-master, and notify/autospawn sections TOMBSTONED rather than deleted so C1–C6, N2/N3 and A1/A4 stay on the record with the dead `.env.local` pair noted as a non-urgent founder act; NO new D-number by design — D-046 decided the retirement and D-048 superseded its last function, so this executed a standing decision, the reasoning written into the spec so no future seat hunts for a decide that was never meant to exist, and every D-046 anchor verified byte-identical by extracting and comparing slug SETS rather than by eye; one file beyond the mandate's list caught, annotated, flagged at the gate and APPROVED — parallel-lanes still armed the clerk as BOTH the away-surface fallback AND the notification watcher, the second the dangerous half because a fallback that cannot fire fails loudly while a notification channel that cannot fire fails silently, and it would have passed the bench's own grep, which said "fire" where that file says "arm": ruled a GAP IN THE MANDATE, not an overreach, with the dormant-baton case reassigned to the cockpit on the same pass; ship §6's critic caught a weave-rule breach in the new IDEAS entry (and taught that a snapshot review can go stale mid-branch — two of its three findings were already fixed by a self-review commit that landed while it ran), external Web review PASS on `0fe69a9` with the gates re-run independently → workshop/mechanism · #197
- 2026-07-22 16:36 · cockpit-resilience — a cockpit that survives, announces, and replaces its own GitHub connector loss (D-048): the research reframed the problem before the fix was written — a cloud session gets a SESSION-SCOPED GitHub MCP injection and no `gh` CLI BY DESIGN, so the connector is the sole API path and redundancy INSIDE a session is impossible, which is why resilience became a ladder OUT of the session rather than a second credential; five rungs into the cockpit charter master — prevent (poll on a cadence, the cost stated, a mitigation not a guarantee) · detect (one cheap probe before ANY command act) · repair (retry once, then shell revival with `claude mcp --help` first and no guessed flags, then a fixed-form warning as its own whole turn) · degrade (git-only work continues, the #193 map cross-referenced not restated) · self-rescue (push one empty commit to `ops/summon`, then land) — plus supersession made loud: a tombstone, a refusal guard that re-derives from origin and answers nothing else, and a successor duty to repaint the board, so exactly one cockpit is ever live; `.github/workflows/summon.yml` ships LIVE on `workflow_dispatch` + a push to `ops/summon`, reusing `scripts/fire.mjs` as-is with secrets from repository secrets, status and session URL to the step summary, and a guarded ref-delete so one push is one rescue; merge-on-signal REJECTED with reasons — it restores only merge, and since every session pushes as the founder a push-triggered merge cannot tell the baton-holder from a lane or a redelivered webhook, breaking no-solo-approval structurally, where a push-triggered SUMMON survives the same test because its worst case is recoverable noise — and a scoped cloud-environment token rejected on the same ground; the fire step's URL extraction moved from `grep -o` to POSIX `sed` after grep proved untestable at the seat, then was exercised on four paths against a stub; the pushed-ref residual accepted and recorded in both homes at the founder's gate fold → workshop/mechanism · #195
- 2026-07-22 15:09 · flight-hardening — the repo stops telling a future seat to do things that cannot work: nine corrections drawn from the first end-to-end chain flight, one theme, no new machinery — the airborne ack token gets ONE canonical form owned by §Canary (`airborne · <url> · <date>`, TEMPLATE and the lane-worker charter master copying it) with the match ANCHORED to the start of the Status line, after a flight where the watcher first matched its own claim prose and then missed an em-dash ack, staged a stand-down, and was saved only by the wake-lock's rejected push; liftoff §6 rung 1's pty-wrapper recipe — which cannot run, the capture being exactly what the wrapper refuses — replaced by the console-attach shape that flew (no redirection, prompt as a file-read argument, output read over `CONOUT$`, console killed after), rungs 2–4 untouched; the DASHBOARD becomes the authoritative flight plan with the birth prompt demoted to a pointer (this flight's mandate arrived truncated mid-sentence); a git-only vs API-only dependency map for the cockpit with a four-step recovery rung, the merge-on-signal Action noted STAGED and deliberately unbuilt; the cloud environment corrected to `Default` with no `gh` and no way to install it (403 → exit 100); one LAWS sentence fixing "non-author" to the payload diff; `.gitignore` gaining `.local/`; a verify-before-rely `[COCKPIT]` title line; and IDEAS triaged six closed, two partly closed. Two out-of-mandate catches approved at the gate (SETUP's identical dead recipe, liftoff §5's third token variant); the founder's bare "merge" held until the external Web review the no-solo-approval law requires of a tower-authored payload returned PASS on `d118af5` → workshop/mechanism · #193
- 2026-07-22 16:19 · lane-worker-baton — the lane-worker charter's canary line names the baton-holder (the D-046 vocabulary sweep's one missed straggler): the fenced "You are a Roam cloud lane" box in SETUP now waits for the baton-holder's airborne ack per §Canary, not "the control tower's" — the six other ground-meaning "control tower" mentions left intact; flown as the first end-to-end flight of the assembled chain (bench birth → label-spawn → canary → cockpit airborne ack → lane edit → non-author cockpit review → the founder's word), the wake-lock catching a mistimed em-dash-vs-middot ack in flight and the TEMPLATE↔§Canary handshake-token disagreement logged to IDEAS → workshop/mechanism · #191
- 2026-07-21 14:56 · cloud-born-cockpit — the cockpit's birth vehicle becomes `claude --cloud` (D-047, amending D-046 clause 3): the automated hidden-console `--cloud` birth is liftoff §6's primary rung — list-native, the sessions join the phone's Code-tab GENERAL list by gate-0c evidence — with compose-and-hand, the routine fire (kept as the staged summon button's engine), and the manual charter paste as the fallback rungs; every composed flight plan opens with the standing clone-provenance first line; three STOP-gates proved clone-from-GitHub and branch-create by a live probe (the bundle ghost dead by evidence, not docs); the mandate run by the probe session itself on the founder's in-list word, external Web review before the weld → workshop/mechanism · #187
- 2026-07-20 22:01 · harness-vocab-rename — the `.claude/` harness learns the D-046 vocabulary: the pickup stub's description and the session-start hook's briefing directive name the BATON-HOLDER (control tower on the ground, cockpit in flight), wording only, zero stragglers by grep; flown as a label-spawned cloud lane, welded from the same seat on the founder's direct word with the reviewer critic's clean verdict standing in as the independent review → workshop/mechanism · #180
- 2026-07-20 15:40 · flight-cockpit — the cockpit is the control tower online (D-046): a cloud command session with FULL authorship — authors benches, births lanes, external-reviews lane PRs, merges on the founder's word, repaints the board — fired by liftoff with the board-derived flight plan as payload; the no-solo-approval law into LAWS (independent review before every merge word, external Web review for self-authored diffs — #175 the precedent, this weld its own first subject); the CC-direct surface doctrine (the paste-block relay a tool, not a requirement; Web's one mandatory job = the external review); clerk retirement staged on the cockpit maiden; Remote Control demoted to backstop; the cockpit/control-tower rename across living doctrine with the BATON-HOLDER (control tower on the ground, cockpit in flight) as the lane-command actor, folded to closure through one external-review amendment and three critic passes; fire.mjs generalized (clerk | cockpit), failure paths repro'd honest both targets → workshop/definition · #177
- 2026-07-20 13:17 · shakedown-audit — the Shakedown Flight (phases 1–2) closes on paper: the A/N checklists graded evidence-or-attest (A1·A2·A3·N2·N3 green — A2 hedged as given; A4's proxy-blindness confirmed; A5 + the glance + the clerk self-report pending founder attest, never invented), six forensics findings closed — the fire-1 400, the exit-127 libuv assert repaired to an honest 1, the shared-SETUP construction that held, the cloud-proxy 403 rail, the resurrection incident with its verify-the-branch-stays-dead ripple, detached-verification-is-not-verification — both staged clerk lines resolved verified per the cloud-clerk precedent, liftoff's fire:clerk line folded in on the founder's gate word, and the attestation haze recorded as lived evidence for the single-surface redesign → workshop/mechanism · #175
- 2026-07-17 23:43 · agent-teams-brain — the Hands doctrine (D-045): solo · exploratory subagents · agent team · parallel lanes, the litmus one-bench/many-hands vs many-benches vs read-only — the founder's passage verbatim into SETUP §Models & effort, D-045 into DECISIONS (lane-written decide, D-044 precedent), one pointer line in parallel-lanes §Vehicles, all three in one ripple commit; flown fully unattended as payload A of Shakedown phase 2 → workshop/definition · #170
- 2026-07-17 23:39 · check-memory — the memory-format CI gate: `scripts/check-memory.mjs` (zero-dep, check-ledger's shape and voice) validates every docs/memory/*.md against TEMPLATE's locked format — frontmatter, the six headings in order, a dated Status, no surviving placeholders — one honest failure line per file, nonzero exit; wired into package.json, ci.yml after check:ledger, and ship §1's mirror; flown fully unattended as payload B of Shakedown phase 2, the two declared doc mentions landed at the weld → workshop/mechanism · #171
- 2026-07-17 16:41 · vercel-ignore-fix — the ignore step fails toward build, never toward error: `|| exit 1` hardens the docs-only skip against Vercel's shallow-clone horizon, where a beyond-clone previous SHA exited 128 and turned four productions ERROR (#153's "failure direction is always build" held for exit 1, not 128 — a shared miss by author, critic, and external review, corrected); documented side-effect: a beyond-horizon docs-only push builds once and self-heals → workshop/mechanism · #167
- 2026-07-17 16:22 · clerk-autospawn — liftoff ignites the clerk by API: fire-clerk.mjs + `fire:clerk` against the doc-verified routine-fire endpoint (per-routine token, dated experimental beta header, no idempotency — no auto-retry), the second routine's recipe + the machine-local secret path, the manual paste retained as fallback; API fires count against the daily cap yet stay invisible to `count:runs`, so liftoff budgets both (A1–A5 grade at the Shakedown Flight) → workshop/mechanism · #164
- 2026-07-17 16:16 · clerk-notify — the clerk gains the standing watch (charter v2, duty 6): on "arm the watch" lane events reach the founder's phone as turn-end announcements — `BLOCKED:` comments, completions/ready-flips, CI red on main; the watcher line opens in the mail slot (in verification, N1–N6 grade at the Shakedown Flight); the doorbell-mirror idea superseded; the reviewer agent-type failure graduated to defect → workshop/mechanism · #163
- 2026-07-17 15:26 · cloud-clerk — the founder's away-mode concierge goes live: the charter master + the C1–C6 maiden flown founder-run, all green (~4.5h idle survival proven, the provider run-count attest closed at 1), the promotion clause executed — clerk PRIMARY for machine-off answering, the GitHub app demoted to backstop — and clerk-notify + clerk-autospawn staged beside api-ignition → workshop/mechanism · #156
- 2026-07-17 11:09 · ship-wiring — the pre-GATE critic wired in (D-044): ship §6 opens by invoking the reviewer subagent, verdicts advisory riding to the founder with the summary; the critic's maiden wired run flew on this very PR (pass + the verdict-as-message clause) → workshop/mechanism · #159
- 2026-07-16 23:55 · handoff-anywhere — leave at any instant, nothing lost: the nine-row mid-state audit proves every interruption parks clean; watch-duty named at park ("watching #N for X") + pickup's re-arm mirror, the unanswered-BLOCKED Needs-you surface ("lane #N awaits your reply"), the interrupt doctrine in one home (Esc lawful anywhere but THE WELD's atomic commit) → workshop/mechanism · #155
- 2026-07-16 23:12 · maiden-flight-report — the delegation maiden flight closed on paper: D-043 (route ladder v2 — ready-flip-then-label the recipe of record, api-ignition + the cloud clerk staged, the Claude app the single away surface), the maiden verify checklist filled, §Answering a lane opened, squash-only + branch auto-delete enforced, the Vercel docs-only build skip live-fired both ways → workshop/mechanism · #153
- 2026-07-16 22:36 · reviewer-subagent — the ship-time diff critic born: spec + `.claude/agents/reviewer.md` (read-only tools, advisory verdicts riding to THE GATE, Sonnet 5 · high) — flown end-to-end by the maiden flight's first live cloud lane, the spawn recipe proven: ready-flip, then label → workshop/mechanism · #146
- 2026-07-16 17:59 · time-doctrine — Time is derived, never recalled: the derivation law gains its time clause, ship/handoff stamps read the shell clock, and the Models & effort doctrine set to the founder's 2026-07-16 statement (reliance pair · the xhigh plateau) — flown end-to-end by a local lane, the maiden flight's leg B → workshop/definition · #147
- 2026-07-16 12:46 · full-pass-fixes — the July full-pass audit closed in one pass: external-item clearing, the routine saved-prompt master, the count:runs cap read, rejected-push wake + label idempotency, the reply-ack window, the maiden-flight verify list, the Models & effort doctrine, README + Web currency → workshop/mechanism · #144
- 2026-07-16 10:37 · lane-liveness — Lane liveness (D-042): live-vs-reclaimable derived from the commit heartbeat and read at the claim check and pickup's worktree sweep, fed by the session-start hook's verdict — a live lane is never adopted or pruned → workshop/mechanism · #142
- 2026-07-16 08:57 · ledger-integrity — a CI gate (check:ledger) proving history/ files and the ledger index stay in one-to-one bijection by #PR, plus a ship §7 weld-staging line so a dropped or orphaned ledger line turns the build red instead of leaving a silent gap → workshop/mechanism · #140
- 2026-07-15 15:35 · cap-confirm — the Max routine cap firmed to confirmed fact (15/day, flat across Max tiers): the SETUP and liftoff live-number hedges retired → workshop/definition · #138
- 2026-07-15 14:39 · delegation-architecture — Delegation architecture (D-041): the away-mode chooser (local · handoff · go-remote · liftoff), the go-remote tether posture, idle-wait, label-spawned cloud lanes → workshop/mechanism · #136
- 2026-07-15 12:58 · laws-tightness — LAWS tightness (Option C): command + one-line whys (D-027 upheld), procedure grain expelled to handoff §1.5, the stale decide trigger and preview conditional fixed → workshop/definition · #134
- 2026-07-15 12:08 · retroactivity-sweep — Retroactivity sweep: repair three later-found gaps — HOME's surviving Cloud-ledger ghost, handoff's non-vocabulary "waiting", recall's FOUNDATION + DESIGN-KICKOFF routing omissions → workshop/definition · #132
- 2026-07-15 11:18 · home-currency-pass — HOME currency pass: bring the bible current with D-040/D-032/D-039/#128 and close six newcomer-test gaps (four rewordings · five new Terms · the recall read-path paragraph) → workshop/definition · #130
- 2026-07-15 10:24 · skills-precision-pass — Skills precision pass: codify already-decided behavior across the corpus (decide · handoff · liftoff · parallel-lanes · recall); the abort-ledger ghost fully retired → workshop/mechanism · #128
- 2026-07-15 09:23 · handoff-inline-context — Handoff input inversion: the leaving message carries the Web/Design paste inline; the never-skipped question is retired; a bare trigger means none → workshop/mechanism · #126
- 2026-07-14 14:50 · recall-skill — Recall, the routing table's read mirror: founder questions answered from the files with receipts, never from memory; fires on its own judgment → workshop/mechanism · #123
- 2026-07-14 11:46 · restyle-sweep — leg-end restyle sweep: every living doc carries its links below the prose → workshop/definition · #121
- 2026-07-14 10:55 · auto-merge-flip — repo auto-merge enabled so the self-merge law works as written; the repo's first armed --auto → workshop/mechanism · #119
- 2026-07-14 10:32 · ci-trust — CI is the arbiter: Actions-green at every gate; the local gate mirrors all six CI steps; anchors born resolving → workshop/mechanism · #117
- 2026-07-14 10:24 · laws-close — Leg close: LAWS carries the routing clause; pickup speaks the founder's shape → workshop/definition · #115
- 2026-07-13 23:14 · home-knowledge — HOME closes the leg's knowledge layer: the routing table, one day in the workshop, the board paragraph recut, a stable Sessions anchor → workshop/definition · #113
- 2026-07-13 22:58 · state-surfaces-v2 — State surfaces v2: the board learns the founder's names; pickup becomes the sit-down summary; welds stamp time and write the ledger → workshop/mechanism · #110
- 2026-07-13 · history-quadrants — History organizes by issue-door quadrants; TEMPLATE owns the memory format and Status vocabulary → workshop/definition · #108
- 2026-07-13 · telemetry-fold — TELEMETRY folds into FACTS: Appendix C; file retired → workshop/definition · #106
- 2026-07-13 · fleet-continuity — Fleet continuity: handoff parks every local lane; liftoff respawns parked benches; wake-lock parks every outcome → workshop/mechanism · #104
- 2026-07-13 · stale-branch-hygiene — Stale-branch hygiene: gone-guard on the safety net; auto-delete welded-elsewhere locals; pickup restyle → workshop/mechanism · #101
- 2026-07-13 · setup-consolidation — Setup consolidation: SETUP.md born; equipment-plan and PROJECT-POLICY retired; the writing laws adopted → workshop/definition · #99
- 2026-07-13 · design-kickoff-refresh — DESIGN-KICKOFF refresh: June-2026 Claude Design capabilities → workshop/definition · #97
- 2026-07-13 · engine-recut — Consolidation recut: DECISION-POLICY becomes ENGINE; PROJECT-POLICY to house style → product/definition · #95
- 2026-07-13 · vault-lens-seed — Vault-lens seed: Obsidian config travels through git → workshop/mechanism · #91
- 2026-07-13 · laws-polish — LAWS polish: glossed lane law, provenance to consolidations, ship syncs with main → workshop/definition · #89
- 2026-07-13 · roadmap-recut — ROADMAP recut: plain-language V1, completion criteria, per-family vetting outputs → product/definition · #87
- 2026-07-12 · foundation-v4 — FOUNDATION v4: principles recut, open family set, lifespan repair → product/definition · #85
- 2026-07-11 · vault-lens — vault lens + clean-tree verdict (ordeals 2+3 close-out) → workshop/mechanism · #82
- 2026-07-11 · web-instructions — canonize the Web-surface rule-carrier → workshop/definition · #80
- 2026-07-11 · phase5-sweep — hardening + hygiene (final ops phase) → workshop/mechanism · #78
- 2026-07-11 · home-encyclopedia — HOME v3: the encyclopedia → workshop/definition · #76
- 2026-07-11 · The engine swap: architecture v2 → workshop/mechanism · #71
- 2026-07-10 · The plan gets a version ladder; identity and plan split by lifespan → product/definition · #69
- 2026-07-10 · Switching computers becomes pull + a checklist → workshop/mechanism · #66
- 2026-07-10 · Design sessions get a rule-carrier; the log is made whole → workshop/definition · #64
- 2026-07-10 · Rituals count for themselves; the ship-tail goes quiet → workshop/mechanism · #62
- 2026-07-10 · Progress gets a face: the stage-map dashboard → workshop/mechanism · #60
- 2026-07-09 · The knowledge layer: the vault learns to explain itself → workshop/definition · #58
- 2026-07-09 · Repo hygiene: line endings pinned, old docs woven → workshop/definition · #57
- 2026-07-09 · Ritual engine v2; the shiplog is born → workshop/mechanism · #56
- 2026-07-09 · Session hooks that work on every machine → workshop/mechanism · #53
- 2026-07-09 · Parallel lanes go native → workshop/mechanism · #51
- 2026-06-12 · Erratum: where cloud lanes are watched → workshop/mechanism · #48
- 2026-06-12 · Lane launches get a recipe that actually works → workshop/mechanism · #46
- 2026-06-12 · What happens after lanes finish → workshop/mechanism · #43
- 2026-06-12 · Cloud sessions become the lane vehicle → workshop/mechanism · #41
- 2026-06-12 · Every briefing ends with a parallel-ready menu → workshop/mechanism · #39
- 2026-06-12 · Weather sources vetted; SOURCES.md is born → product/definition · #37
- 2026-06-12 · The system learns to record itself → product/definition · #35
- 2026-06-12 · Traveler vocabulary grows 26 → 47 fields → product/definition · #33
- 2026-06-12 · The fact inventory; FACTS.md is born → product/definition · #31
- 2026-06-11 · Roam has a database → product/build · #29
- 2026-06-11 · The pacing law → workshop/definition · #27
- 2026-06-11 · Roam is on the internet, with a quality gate → product/build · #25
- 2026-06-11 · The cockpit: HOME.md → workshop/mechanism · #23
- 2026-06-11 · Tooling scope pinned → workshop/definition · #21
- 2026-06-11 · The app exists → product/build · #19
- 2026-06-11 · Subagent plan, stage-gated → workshop/definition · #17
- 2026-06-11 · Equipment plan, stage-gated → workshop/definition · #15
- 2026-06-11 · Blueprint audit fixes → product/definition · #13
- 2026-06-11 · The full V1 build plan → product/definition · #11
- 2026-06-11 · Stack re-trial: choice upheld, frontend named → workshop/definition · #9
- 2026-06-11 · The product is defined on paper → product/definition · #7
- 2026-06-11 · Handoff notes get a merge carve-out → workshop/definition · #5
- 2026-06-11 · Friendly machine labels → workshop/definition · #3
- 2026-06-10 · Context engine v2 → workshop/definition · #2
- 2026-06-10 · The workflow kit → workshop/mechanism · #1
