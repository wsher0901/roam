---
type: spec
id: chronicle-layer
title: The chronicle layer — living stories above the frozen record, and the census that shelves them ([D-059](../DECISIONS.md#d-059--the-chronicle-layer))
status: shipped
stage: workshop
branch: docs/chronicle-layer
pr: 233
opened: 2026-07-27
shipped: 2026-07-27 21:34
---

# chronicle-layer — the story layer is decided; the census drafts its shelves

**Links:**
[D-059](../DECISIONS.md#d-059--the-chronicle-layer)
· queue: the founder's 2026-07-27 three-bench mandate (bench 3 of
3, after the record shelf
([#229](https://github.com/wsher0901/roam/pull/229)) and ATLAS
([#231](https://github.com/wsher0901/roam/pull/231))).

## Goal

Two deliverables, no story written. FIRST, the decision: the
chronicle layer's six rulings, verbatim from the ops chat
(2026-07-27) — one living story per ITEM; the ending rule
(settled ⇒ written; later fixes incorporated, never a second
story); `docs/chronicle/` with `INDEX.md` shelving four shelves
(the away surface · the lane system · the information system ·
Roam); immutability beneath (record/ untouched, stories link DOWN
as footnotes); down-links only (a story never cites another
story's prose); supersession of the three-chapter shape queued in
[IDEAS](../../IDEAS.md) on 07-23. SECOND, the census:
`docs/chronicle/INDEX.md` born as a DRAFT — the proposed story
list per shelf, produced by the workshop's FIRST agent-team use
(three teammates, one shelf each, exploration only; the lead
carries the Roam shelf), each proposal carrying item · the
PRs/decides it spans · its current ending in one sentence ·
confidence. The census rides the gate report for the founder's
veto.

## Out of scope

- Writing any story — no `docs/chronicle/` file beyond `INDEX.md`
  is born on this bench.
- The ship-ritual synthesis-at-weld step — deferred by the
  founder's ruling to the first story weld (its own bench,
  touching ship.md).
- Inventing an ending the record cannot derive — the T3–T6
  batch-1 arc (four lanes launched 06-12, zero landed, no
  post-mortem on record) is marked "founder input owed", never
  filled.
- Teammates writing to the tree — they return proposals only.

## Plan

1. Bench birth: branch, this spec, memory stub, draft PR.
2. Mint the D-number — the six rulings verbatim; ripple: HOME
   gains one routing-table line (stories → chronicle/), the IDEAS
   07-23 line closes citing the decide.
3. THE CENSUS, per [HOME §Agent teams](../../HOME.md#agent-teams)
   and the [D-056](../DECISIONS.md#d-056--team-aware-leaving-rituals)
   leaving duty: a ground-seat team, one sitting, exploration
   only — three teammates (away surface · lane system ·
   information system), read-only toolsets, each reading the
   ledger, its shelf's record/history files, and
   record/DECISIONS.md; the lead reads the Roam shelf itself (14
   product stories). Teammates return proposed stories; the lead
   composes `docs/chronicle/INDEX.md` as the draft census.
4. Capture the team's findings into THIS bench's memory, then
   shut the team down BY NAME, then report what each returned
   (capture, then kill).
5. Verification loop, full CI mirror, Actions green, ship §6
   critic, gate report carrying the census for the veto.

## Done means

- [x] The D-number entry exists with all six rulings verbatim in
      substance, anchors born resolving.
- [x] `docs/chronicle/INDEX.md` exists as a draft census — four
      shelves, every proposal carrying item · span · one-sentence
      ending · confidence; underivable endings marked "founder
      input owed" (the T3–T6 batch-1 arc among them).
- [x] [HOME](../../HOME.md) carries exactly ONE new routing-table
      line; the [IDEAS](../../IDEAS.md) 07-23 line is closed
      citing the decide.
- [x] The team's findings are captured in the bench memory
      (pushed BEFORE the shutdown requests, per the external
      reviewer's sequencing correction); shutdown requested from
      each teammate by name, each return reported — the
      no-response state at gate time named honestly in the
      memory Status.
- [x] Full CI mirror green; the pushed head's Actions run green;
      ship §6 critic run. record/ untouched except the DECISIONS
      append (verified by the critic against the branch diff).

## Deviations

none in the plan itself; disclosed at the gate: (1) the critic
found the INDEX header restating all six rulings — a living copy
of a frozen entry — compressed to a pointer plus the two rules
its reader operates by (one-home); (2) one typed derived count in
the census ("98 stories") re-stamped as a dated census figure per
the derivation law, and one memory stamp corrected to an honest
clock read; (3) `docs/chronicle/` has a routing row but no row in
[HOME §The files](../../HOME.md#the-files--what-each-one-is-for)'s
catalog — the mandate's one-routing-line cap excluded it, the
exact class the founder licensed for ATLAS at its external
review; surfaced for the same ruling; (4) the teammates' shutdown
responses had not arrived by gate time — reported honestly, risk
bounded (read-only agents, deliverables already captured and
pushed).

## Open questions

none — the census's story list itself is the founder's veto
surface at the gate.
