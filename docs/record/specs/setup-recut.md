---
type: spec
id: setup-recut
title: SETUP compressed to the entry contract — bench 2 of the SETUP recut
status: open
stage: workshop
branch: docs/setup-recut
pr: —
opened: 2026-07-29
shipped: —
---

# setup-recut — the container becomes a replication spec

**Links:**
[SETUP](../../SETUP.md) (the file being recut) ·
[D-064](../DECISIONS.md#d-064--2026-07--the-box-master-class--the-setup-entry-contract--every-external-box-master-is-its-own-top-level-file-on-the-web-instructions-pattern-cockpit-chartermd-and-lane-workermd-extracted-verbatim-setup-compresses-to-a-replication-spec-under-the-entry-contract-what--where--values--verify--source-design-kickoff-joins-the-class-on-paper)
(the decide this bench executes — no new D-number) ·
[bench 1](../history/workshop/definition/box-masters.md)
([#242](https://github.com/wsher0901/roam/pull/242)) · source: the
founder's approved plan, Web ops chat 2026-07-29.

## Goal

[D-064](../DECISIONS.md#d-064--2026-07--the-box-master-class--the-setup-entry-contract--every-external-box-master-is-its-own-top-level-file-on-the-web-instructions-pattern-cockpit-chartermd-and-lane-workermd-extracted-verbatim-setup-compresses-to-a-replication-spec-under-the-entry-contract-what--where--values--verify--source-design-kickoff-joins-the-class-on-paper)
decided the SETUP ENTRY CONTRACT and bench 1 cleared the two
masters out. This bench executes the rest: every entry compressed
to WHAT · WHERE · VALUES · VERIFY · SOURCE, the retired clerk
collapsed to one tombstone, and the cockpit's WHY — the API
dependency map and its recovery-rung narrative — moved to
[HOME](../../HOME.md), where explanations live.

THE ACCEPTANCE BAR, per entry: a fresh machine plus SETUP alone
reproduces the configuration with ZERO GIT ARCHAEOLOGY.

## Out of scope

- **A new decide.**
  [D-064](../DECISIONS.md#d-064--2026-07--the-box-master-class--the-setup-entry-contract--every-external-box-master-is-its-own-top-level-file-on-the-web-instructions-pattern-cockpit-chartermd-and-lane-workermd-extracted-verbatim-setup-compresses-to-a-replication-spec-under-the-entry-contract-what--where--values--verify--source-design-kickoff-joins-the-class-on-paper)
  already records this bench; [DECISIONS](../DECISIONS.md) is not
  touched.
- **The board.** The dead `CLERK_` credentials line is
  founder-facing status, so it leaves SETUP and reaches
  [DASHBOARD](../../DASHBOARD.md) Needs-you via the ship tail —
  not by this bench editing the board.
- **Retrofitting [WEB-INSTRUCTIONS](../../WEB-INSTRUCTIONS.md).**
  It keeps its laws inside its fence deliberately; the reasoning
  is recorded in this bench's memory.
- **Editing the frozen record**, beyond adding any new file that
  receives orphan clerk narrative.

## Plan

1. Bench birth: branch, this spec, memory stub, draft PR.
2. SETUP's header prose replaced by the entry contract verbatim,
   D-064 woven; frontmatter, title and Sources kept.
3. Every entry compressed to the contract, with a VERIFY probe
   where one exists and an honest "VERIFY: none — UI-only" where
   none does. THE STANDING DUTY: before any sentence is deleted,
   the fact it carries is confirmed to exist at the pointer's
   target; a fact with NO home stays, or moves to the record.
4. The clerk's three entries, charter block and spawn preamble
   collapse to ONE tombstone; unique narrative relocates to the
   record and is named in the memory.
5. The WHY moves: the API dependency map + recovery rungs join
   [HOME](../../HOME.md)'s cockpit material; SETUP keeps one
   pointer. CASCADE in the same bench —
   [COCKPIT-CHARTER](../../COCKPIT-CHARTER.md)'s R3 line
   re-pointed at the new HOME anchor, its version table bumped to
   v2 with a changelog row.
6. Destination-grep every living link into SETUP §cloud accounts;
   re-point those whose target content moved. The frozen record
   stays untouched.
7. The review's two riders:
   [HOME §Where information goes](../../HOME.md#where-information-goes)
   gains one routing line for the box-master class; the
   WEB-INSTRUCTIONS reasoning is recorded in the memory.
8. Verification loop, full CI mirror, Actions green, ship §6
   critic; ship to THE GATE. Tower-authored — external Web review
   precedes the founder's word.

## Done means

- [ ] SETUP's header carries the entry contract verbatim with
      D-064 woven; frontmatter, title and Sources intact.
- [ ] Every entry follows WHAT · WHERE · VALUES · VERIFY ·
      SOURCE, keeps EVERY non-default value, and carries a VERIFY
      probe or an honest "none — UI-only".
- [ ] No fact was deleted without a confirmed home; anything
      unique that left SETUP is named in the memory with where it
      went.
- [ ] The clerk is ONE tombstone entry; its charter text and
      spawn preamble survive at a named record location.
- [ ] The dependency map + recovery rungs live in
      [HOME](../../HOME.md); SETUP keeps one pointer;
      [COCKPIT-CHARTER](../../COCKPIT-CHARTER.md)'s R3 line points
      at the new HOME anchor and its version table reads v2 with a
      changelog row.
- [ ] Every living link into SETUP §cloud accounts whose target
      content moved is re-pointed; the frozen record is untouched.
- [ ] [HOME §Where information goes](../../HOME.md#where-information-goes)
      carries the box-master routing line.
- [ ] THE ACCEPTANCE BAR holds: each entry reproduces its
      configuration from SETUP alone.
- [ ] Files touched: nothing outside the mandate's list;
      DECISIONS and DASHBOARD untouched.
- [ ] `check:links` · `check:ledger` · `check:memory` · lint ·
      format · test · build green; Actions green on the pushed
      head; ship §6 critic run.

## Deviations

<filled by ship>

## Open questions

none at birth.
