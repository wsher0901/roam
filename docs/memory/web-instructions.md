---
type: memory
id: web-instructions
updated: 2026-07-11 · birth · home PC
---
# web-instructions — canonize the Web-surface rule-carrier

## Status
Bench ready — branch, this stub, and the draft PR are born. BLOCKED
immediately at birth: the body must be the founder-approved v4 text
from the Web chat, VERBATIM, and that text has not been pasted into
the cockpit. Asked the founder; nothing is written until it arrives
(the [engine-swap](../history/engine-swap.md) precedent — never
reconstruct a founder-approved text from memory).

## What this task is
Create docs/WEB-INSTRUCTIONS.md as the master copy of the Claude
Web Project-instructions box — the same rule-carrier pattern as
[DESIGN-KICKOFF](../DESIGN-KICKOFF.md): edit in the repo via PR,
re-paste into the box, the box is a copy and never the source.
Frontmatter type: rule-carrier; one preamble line; the v4 text in
one fenced block (a paste artifact, fence-exempt from the weave).
Ripples in the same commit: the [HOME](../HOME.md) files table and
the four-tools Surfaces mention gain the WEB-INSTRUCTIONS row/line,
mirroring DESIGN-KICKOFF. Verification: `npm run check:links` +
valid frontmatter. Ship to THE GATE; DO NOT MERGE — founder
approval follows the external Web-chat review.

## Pending issues
The v4 text itself — only the founder can carry it across from the
Web chat.

## Left / idle
Everything content-side, pending the paste: the file, the two HOME
ripples, verification, ship.

## The story
A micro task from the same ops lineage as
[phase5-sweep](../history/phase5-sweep.md), born the moment after
it shipped. Birth artifacts went up clean; the task blocked at its
first content step by design — the Web surface's text lives only in
the founder's browser, and the workshop's one manual link (the
founder's paste) is the sanctioned carrier.

## Where to look
The draft PR · the ops Web chat (source of the v4 text) ·
[DESIGN-KICKOFF](../DESIGN-KICKOFF.md) (the pattern sibling).
