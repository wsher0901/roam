---
type: memory
id: web-instructions-v6
updated: 2026-07-29 09:09 · external review repairs · work PC
---
# web-instructions-v6 — the Web surface rewritten, the doctrine recut

## Status

complete, awaiting merge — 2026-07-29, work PC, after the external
review's repair round. All four original edits are in (the
master's fence with its version-history table,
[HOME §Response doctrine](../HOME.md#response-doctrine)'s seven v2
rules, D-063, the [IDEAS](../IDEAS.md) sweep line), and the
review's five repairs on top: ⏳ admitted to the glyph set in BOTH
homes, the ROADMAP-stage guardrail revived into Agenda, the
explicit-go law written into Prompt delivery, the review's prose
ban widened to verification narrative, and the cockpit charter's
rule 5 gloss reduced to a pure pointer. THE FOUNDER'S GAP RULING
IS RECORDED (below). Full CI mirror green. Back at THE GATE: the
external review re-checks this tip before the founder's word. DO
NOT MERGE.

## What this task is

The Web surface's instruction master goes v5 → v6, and the
response doctrine goes v1 → v2 with it — two channels (sentences
for the founder, fenced blocks for machines), process as proof
rather than prose, plus the chat-start ingest contract, the relay
rule, the review's goal-and-outcome output, the core-file alert,
the agenda convention, a full-picture brief, and the master's own
full-copy and changelog laws. Contract:
[the spec](../record/specs/web-instructions-v6.md).

## Pending issues

- Tower-authored, so
  [no-solo-approval](../LAWS.md#workflow-non-negotiable) requires
  an external Web review before the founder's word at THE GATE. DO
  NOT MERGE.
- **Two founder acts follow this bench, and one of them is
  already owed.** (1) The Web Project-instructions box must be
  re-pasted from the master — v6 makes that its own law (FULL
  COPY, ALWAYS), and the version line in the box is what a future
  chat drift-checks against. (2) The COCKPIT ROUTINE BOX must be
  re-saved, because this bench edited the charter master again
  (rule 5's gloss). That re-save is ALREADY OWED from
  [#238](https://github.com/wsher0901/roam/pull/238)'s charter
  change and sits on the board's Needs-you; ONE re-save covers
  both, so no new item is created — it keeps riding the existing
  one.
- **THE GAP IS RULED** (2026-07-29) — see the story. Nothing owed.

## Left / idle

nothing parked.

## The story

Born 2026-07-28 at the work PC, the third bench of the sitting
after [the nine owed endings](../record/history/workshop/definition/chronicle-answers.md)
and [the landing recut](../record/history/workshop/mechanism/land-doctrine-v2.md).

### THE GAP — two v5 sections the v6 text does not carry

The mandate ordered the fence replaced BYTE-FAITHFUL under the
[#80](https://github.com/wsher0901/roam/pull/80) precedent: flag
any gap, never reconstruct. There is a gap, and it is two whole
sections. v5's fence carried ten `##` sections; the approved v6
text carries twelve, but they are not a superset — **v5's
`## Conversation & pacing` and `## Style` appear nowhere in v6**
and were NOT carried over.

What they said, so the founder can rule on it with the content in
front of them rather than from memory:

- **Conversation & pacing** — one chat per topic, with a recap
  and a proposed title when discussion drifts into a distinct
  workstream; chat naming (roadmap IDs, or "Ops —" plus the
  topic); never
  encourage work ahead of its [ROADMAP](../ROADMAP.md) stage;
  finish-first, with recommendations needing the founder's
  attention CAPPED AT TWO; and folding any roadmap-level change or
  new idea into the delivered prompt.
- **Style** — direct, concise, plain language for a solo builder
  without a CS background, "but never bare: keep the load-bearing
  reasoning"; trade-offs as 2–3 options with pros/cons and a
  recommendation; lead with the answer; explain purpose-shaped,
  last.

PARTLY ABSORBED, so the loss is smaller than two whole sections
suggests: doctrine v2's rule 4 (human-readable, plain words, "if
it looks stressful to read, it already failed") covers most of
Style's register, and v6's Prompt delivery keeps "Ideas or defects
voiced here ride the next delivered prompt as IDEAS lines. Never
leave a decision only in chat."

SURVIVING VIA LAWS, which v6 tells the surface to fetch when
unsure — two rules with the IDENTICAL survival path, so they get
the identical verdict (the critic caught the bench grading them
asymmetrically on its first pass): the 2–3-options-with-a-
recommendation rule, in
[LAWS §Who you're working with](../LAWS.md#who-youre-working-with),
and the finish-first cap on what needs the founder's attention, in
the [pacing law](../LAWS.md#workflow-non-negotiable). Both are
reachable but no longer STATED on the Web surface, and a rule the
surface must go looking for is weaker than one in its own box.

NOT ABSORBED ANYWHERE — independently confirmed by the critic,
which found no other home for either: the ONE-CHAT-PER-TOPIC
convention with its naming rule (roadmap IDs, or "Ops —" plus the
topic) — the board only RENDERS chat titles, it never governs
them — and "NEVER ENCOURAGE WORK AHEAD OF ITS ROADMAP STAGE",
which greps to nothing outside the old v5 box.

Nothing was reconstructed. The gap went to THE GATE for the
founder to rule, and it was ruled.

### THE FOUNDER'S RULING (2026-07-29, the Web ops chat)

Three of the four dropped things stay dropped; one comes back.

- **`## Conversation & pacing` stays dropped — INTENTIONAL.** The
  section as a whole is not part of v6.
- **`## Style` stays dropped — ABSORBED.** Doctrine v2 carries its
  register: rule 4 (short sentences, plain words, jargon only when
  load-bearing) is what Style was for, and the founder judged the
  absorption complete.
- **ONE-CHAT-PER-TOPIC DIES**, naming rule with it. It was the one
  loss with no home anywhere, and the ruling is that it does not
  need one.
- **THE ROADMAP-STAGE GUARDRAIL IS REVIVED** — "Never encourage
  work ahead of its ROADMAP stage" returns as the closing line of
  `## Agenda`, which is where the surface already tracks what is
  in play versus queued. It was the only dropped rule the founder
  judged load-bearing.

So the gap closes at one line restored out of two sections, and
the record says which three were let go on purpose rather than
leaving a future reader to wonder whether they were lost.

### The critic round (ship §6, pre-flip)

Ten findings on `e05793f`. The two that mattered were about this
bench's own hygiene rather than its payload, and both are
repaired: the memory still declared itself at its first step while
carrying the finished work, and the spec's Done-means were all
unticked with Deviations unwritten. A third sharpened the gap
analysis above — the bench had graded two rules with the same
survival path differently.

WHAT THE CRITIC COULD NOT CHECK, stated plainly rather than
counted as a pass: it had no shell, so it could not diff against
`main` and could not verify BYTE-FAITHFULNESS of the fence or
independently enumerate the dropped sections — the single hardest
thing the bench needed checked. What it could do it did: it found
no paraphrase seams, and confirmed every cross-reference inside
the v6 text resolves (§Prompt delivery exists, "doctrine rule 6"
is indeed STATE IS DERIVED LIVE, the version line the ingest
contract drift-checks against exists, the changelog the file's
own laws promise exists). The bench's own mechanical check
confirms the structural invariants: the version line reads v6,
twelve `##` sections, seven numbered doctrine rules, the handoff
block present, one fence opened and closed. THE INDEPENDENT
BYTE-CHECK BELONGS TO THE EXTERNAL WEB REVIEW, which holds the
founder's own copy of the approved text — it is the only surface
that can do it, and this bench does not claim it.

THREE FINDINGS ARE OUT OF THIS BENCH'S FILES and go to the
founder rather than being silently fixed:

1. **[the census](../chronicle/INDEX.md) carries three statements
   this weld falsifies** — the response-doctrine row's ending
   ("Settled: one home in HOME"), the WEB-INSTRUCTIONS row ("v5
   lives as the repo master"), and §Founder input owed item 8,
   which records the box re-paste as CLOSED when this master
   change re-opens it. The chronicle's ending rule says a later
   fix is incorporated at the weld that advances the item —
   but ship carries no chronicle step yet, deferred by
   [D-059](../record/DECISIONS.md#d-059--2026-07--the-chronicle-layer--living-stories-above-the-frozen-record-one-story-per-item-the-ending-rule--settled-in-any-format-means-the-story-is-written-a-later-fix-is-incorporated-and-the-ending-rewritten-at-that-weld-retirement-is-a-settlement-docschronicle-one-file-per-story-indexmd-shelving-four-shelves-immutability-beneath--record-untouched-stories-link-down-as-footnotes-down-links-only--a-story-never-cites-another-storys-prose-supersedes-the-07-23-three-chapter-shape-the-ship-synthesis-at-weld-step-deferred-to-the-first-story-weld)
   to the first story weld. So this may be a deliberate wait
   rather than a miss.
2. **The cockpit charter's rule 5 glosses doctrine v1's clauses**
   ("answer first · explicit steps · purpose-shaped explanation
   last · live-derived state · glyph verdicts") while pointing at
   the section that no longer contains them
   ([SETUP §cloud accounts](../SETUP.md#once-and-done--cloud-accounts)).
   The retroactivity law wants that backfilled in the same PR; the
   spec put the report RESTYLE out of scope, but a five-word stale
   gloss is not a restyle.
3. **The v6 text introduces ⏳ for "queued"** in its Agenda
   section, a glyph outside doctrine rule 7's own set
   (🟢/🟡/🔴/⏸) and outside the board legend's ⚪. Inside the
   supplied text, so flagged rather than touched.

### The external review's repair round (2026-07-29)

Five repairs, all on the same branch — no new bench, because the
payload never changed shape:

1. **⏳ ADMITTED TO THE GLYPH SET.** The ship §6 critic had caught
   the v6 text using ⏳ for "queued" in Agenda while rule 7's own
   set stopped at 🟢/🟡/🔴/⏸ — a text inconsistent with itself.
   The review's resolution went the other way from the obvious
   one: rather than strike ⏳ from Agenda, ADMIT it to rule 7, in
   BOTH homes, so the two stay word-identical. Queued is a real
   state and the doctrine had no glyph for it.
2. **The ROADMAP-stage guardrail revived** into Agenda, per the
   founder's gap ruling above.
3. **THE EXPLICIT-GO LAW** replaces v6's softer "Content is shown
   for approval before any block is generated": a proposal and
   its block NEVER share a message, and the block comes only on
   the founder's explicit go. The failure it prevents is agreeing
   to an idea being read as agreeing to the mandate that
   implements it.
4. **The review's prose ban widened** — "no receipts, gate
   numbers, or shas" becomes "no receipts, gate numbers, shas, or
   VERIFICATION NARRATIVE". Naming the shas was never the whole
   problem; narrating the checking was.
5. **The cockpit charter's rule 5 gloss reduced to a pure
   pointer.** It listed doctrine v1's five clauses while pointing
   at the section that now holds seven different ones — the exact
   stale-copy the one-home rule exists to prevent, and the
   [retroactivity law](../LAWS.md#knowledge--tracking) wants it
   backfilled in the PR that caused it. This is the ship §6
   critic's finding 2, which this bench had routed to the founder
   rather than fixing, now ruled and fixed.

The critic's other routed finding — the census carrying three
statements this weld falsifies — was NOT ruled in this round and
stays open for the founder; ship still has no chronicle step
(deferred by [D-059](../record/DECISIONS.md#d-059--2026-07--the-chronicle-layer--living-stories-above-the-frozen-record-one-story-per-item-the-ending-rule--settled-in-any-format-means-the-story-is-written-a-later-fix-is-incorporated-and-the-ending-rewritten-at-that-weld-retirement-is-a-settlement-docschronicle-one-file-per-story-indexmd-shelving-four-shelves-immutability-beneath--record-untouched-stories-link-down-as-footnotes-down-links-only--a-story-never-cites-another-storys-prose-supersedes-the-07-23-three-chapter-shape-the-ship-synthesis-at-weld-step-deferred-to-the-first-story-weld)
to the first story weld), so the wait may be the right answer.

## Where to look

- [the spec](../record/specs/web-instructions-v6.md) — the
  contract.
- [WEB-INSTRUCTIONS](../WEB-INSTRUCTIONS.md) — the master being
  rewritten.
- [HOME §Response doctrine](../HOME.md#response-doctrine) — the
  doctrine's one home.
- the Web ops chat, 2026-07-28 — where v6 was drafted and
  approved.
