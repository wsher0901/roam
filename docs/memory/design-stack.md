---
type: memory
id: design-stack
updated: 2026-08-11 · birth · work PC
---
# design-stack — the global design stack, and Roam's alignment

## Status

bench ready — born 2026-08-11 at the work PC control tower, from
the founder's two-part mandate. Spec is the contract; the branch
carries both halves.

## What this task is

Install a machine-wide design capability at USER scope (the
official `frontend-design` plugin, playwright + shadcn + context7
MCP servers, a global design law in `~/.claude/CLAUDE.md`, and a
global `design-review` agent), then make Roam answer to it: one
decide entry, a new `docs/DESIGN.md` holding Roam's own taste,
a design-review gate for UI-touching diffs, and the setup steps
recorded where a second machine will find them. Contract:
[the spec](../record/specs/design-stack.md).

## Pending issues

None yet.

## Left / idle

Everything — the bench is at birth.

## The story

2026-08-11 · BORN · work PC

The mandate arrived whole, in two parts, and the split matters:
Part A writes nothing to this repo and Part B writes nothing to
the machine. They share a bench because the second is meaningless
without the first — a law pointing at an agent that does not exist
is a law that fails silently.

## Where to look

- [the spec](../record/specs/design-stack.md) — goal, out of scope,
  plan, Done-means.
- [D-007](../record/DECISIONS.md#d-007--shadcnui-builds-on-base-ui) — the UI
  kit choice this stack has to agree with.
- [DESIGN-KICKOFF](../DESIGN-KICKOFF.md) — the Design surface's
  rule-carrier, which gains the two preamble lines.
