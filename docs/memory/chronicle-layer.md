---
type: memory
id: chronicle-layer
updated: 2026-07-27 21:09 · team captures 1–2 of 3 (away-surface · lane-system) · home PC
---

# chronicle-layer — the story layer is decided; the census drafts its shelves

## Status

In work at the control tower — home PC, born 2026-07-27 20:52.
Bench 3 of the founder's three-bench queue (record shelf ✓
[#229](https://github.com/wsher0901/roam/pull/229) → ATLAS ✓
[#231](https://github.com/wsher0901/roam/pull/231) → chronicle
layer). Birth commit: spec + this stub; the decision and the
census come next. No story is written on this bench.

## What this task is

The chronicle layer is decided (the founder's six ops-chat
rulings, minted as the next D-number) and its shelves are drafted:
`docs/chronicle/INDEX.md` is born as a DRAFT census — the only
chronicle/ file this bench creates — listing proposed stories per
shelf (the away surface · the lane system · the information
system · Roam), each with item, span, one-sentence current
ending, and confidence. The census is produced by the workshop's
FIRST agent-team use: three read-only teammates, one shelf each;
the lead carries the Roam shelf itself. Underivable endings are
marked "founder input owed" — the T3–T6 batch-1 arc is the known
case. The census rides the gate report for the founder's veto.

## Pending issues

none

## Left / idle

The decision entry, the census, the team capture-and-shutdown,
verification, the critic, the gate report.

## The story

Born 2026-07-27 from the founder's queue mandate, decided in the
ops chat. The bench's discipline: teammates write NOTHING to the
tree; the record stays untouched except the DECISIONS append; no
ending is invented — what the record cannot derive is owed by the
founder, not guessed. The decision (`D-059`, this bench; the link
weaves in at the landing commit, when its anchor exists on the
branch) and its ripple sit in the working tree awaiting the
one-commit landing with the census INDEX. Sequencing per the
external reviewer's correction: each teammate's findings are
captured HERE and pushed BEFORE the team shutdown, separate from
the landing commit.

### Team findings — capture 1 of 3: away-surface (the away-surface shelf)

Returned 2026-07-27, twelve proposed stories, each with item ·
type · spans · one-sentence ending · confidence · sources:

1. HANDOFF (system piece, high) — spans #1 → #56 → #62 → #104 →
   #126 → #128 → #144 → #155 → #177 → #211 → #216 → #225. Ending:
   the ground-seat leaving ritual with a FULL/QUIET fork — team
   shutdown → secure → park → inline paste → memory rewrites →
   board repaint → note → a close that leaves the session
   read-only rather than dead.
2. LIFTOFF (system piece, high) — spans #104 → #128 → #136 →
   #138 → #144 → #164 → #177 → #187 → #193 → #197 → #204 → #207 →
   #211 → #213 → #216 → #218 → #225. Ending: runs a FULL handoff,
   triages, births benches, canary-verifies, welds the flight
   plan to the board, fires `claude --cloud` blind, closes naming
   rung + exit code but never a cockpit URL.
3. THE COCKPIT (system piece, high) — spans #177 → #180/#191 →
   #193 → #200 → #207 → #213 → #216 → #221 → #225 → #231. Ending:
   the baton-holding cloud command seat — authors, births/acks
   lanes, reviews as non-author, merges on the word, seats
   itself, reports as a fleet table, ends through land.
4. THE COCKPIT'S BIRTH VEHICLE (system piece, high) — spans #177
   → #187 → #193 → #204 → #213. Ending: rung 1 a blind
   hidden-window `claude --cloud` fire (exit status only), with
   compose-and-hand, the routine fire, and the manual paste
   beneath; the greeting push is the aliveness signal.
5. CONNECTOR RESILIENCE (system piece, MEDIUM — rungs 2b and 5
   shipped unproven, never graded later; secrets/workflow never
   recorded live) — spans #193 → #195 → #207. Ending: five rungs
   with two API paths beneath, self-rescue via ops/summon, the
   phone bootstrap last.
6. THE CLOUD CLERK (system piece, high) — spans #153 → #156 →
   #163 → #164 → #175 → #177 → #197 → #200 → #204. Ending:
   retired — the founder deleted the routine 2026-07-22 ahead of
   its staged trigger; tombstoned record only; fire.mjs
   cockpit-only; three attests never obtained.
7. THE AWAY-MODE CHOOSER (system piece, high) — spans #136 →
   #153 → #177 → #221 → #231. Ending: the fork settled and
   depicted; the return runs liftoff → cockpit → land's two
   modes → pickup's fleet resume.
8. GO-REMOTE / REMOTE CONTROL (system piece, MEDIUM — no
   recorded run ever) — spans #136 → #177 → #204. Ending: alive
   as a six-step skill and backstop posture — never the plan
   since the cockpit became the away seat; kept because demoted
   is not dead.
9. LAND (system piece, MEDIUM — shipped, no flight since, so
   apparently never run) — spans #221 → #225 → #231. Ending: the
   cockpit-only ending ritual, two modes, the fence built out of
   the wake-lock, the board the single transfer material.
10. TEAM-AWARE LEAVING (system piece, high) — spans #223 → #225.
    Ending: every ritual road out of a sitting captures a team's
    findings before killing it; the superseded-seat self-close
    stays uncovered and filed.
11. THE SHAKEDOWN FLIGHT 07-17→19 (event, high) — spans
    #163/#164 → #170/#171 → #175. Ending: closed on paper
    07-20, A1/A2/A3/N2/N3 green, three attests honestly pending
    and never obtained; its attestation haze produced the
    single-surface cockpit redesign.
12. THE COCKPIT'S PROVING FLIGHTS 07-22 + 07-23 (event, high) —
    spans #187 → #191 → #193 → #195 → #200. Ending: both flights
    completed and welded, proving the chain while producing the
    correction benches that hardened it.

Shelf-boundary calls (carried as spans only, items claimed by
other shelves): the closed-≠-dead doctrine (#211) and the
response doctrine's ritual templates (#216).

FOUNDER INPUT OWED (away-surface): 1. whether land has ever
actually run, and in which mode. 2. whether the summon workflow's
repository secrets exist and self-rescue is armed today.
3. whether D-048's rungs 2b and 5 were ever tested. 4. whether
D-051's blind-fire + self-seat pair has been exercised together
on a real flight. 5. whether the go-remote tether has ever been
used and Remote Control still exists on the phone — if not, its
ending is retirement-by-platform, not demotion.

### Team findings — capture 2 of 3: lane-system (the lane-system shelf)

Returned 2026-07-27, fifteen proposed stories:

1. THE LANE LAW (system piece, high) — spans D-020/#51 → D-023 →
   D-032 → D-041 → D-042 → D-043 → D-046 (birth actor becomes
   the baton-holder) → #180 → #191 (first end-to-end flight) →
   #193 (non-author pinned to the payload diff) → D-054 → D-055
   (teams not a vehicle) → D-058. Ending: seven clauses in LAWS,
   mechanics in parallel-lanes, seat-blind, guarded by
   seat-invariance.
2. THE CANARY HANDSHAKE + ACK TOKEN (system piece, high) — spans
   D-020 → #144 → D-042 → #191 (the em-dash ack near-miss) →
   #193 (one canonical anchored form, §Canary the sole home) →
   #231 (ATLAS's box repaired, same class). Ending: §Canary owns
   the token and all three timeout constants; anchored prefix
   match; TEMPLATE and the charter master are copies.
3. THE WAKE-LOCK (system piece, high) — spans D-032 → D-041
   (idle-wait carve-out) → #146/#153 (kills a redelivered
   webhook) → #180 → #191 → D-054 (the fence rides it). Ending:
   the backstop under liveness misjudgment and the only thing
   that can stop a running lane.
4. LANE LIVENESS (system piece, high; the #140 collision folded
   in as the raising event) — spans #140 → D-042 → D-054.
   Ending: live = non-terminal Status + heartbeat in window;
   terminal word outranks a fresh heartbeat; read at the claim
   check and pickup §3, fed by the hook's verdict.
5. FLEET CONTINUITY / PARK PROTOCOL / RESPAWN (system piece,
   high) — spans D-032 → D-041 → #155 → D-042 → D-054 → D-056.
   Ending: the park is ONE stamped line at handoff FULL;
   §Respawn covers adoption; pickup §6 owns grounded-fleet
   resume.
6. THE CLOUD SPAWN ROUTE LADDER + LANE-WORKER ROUTINE (system
   piece, high) — spans D-041 → #144 → #146 → D-043
   (ready-flip-then-label, three-point evidence) → #191/#180 →
   #197 → D-054. Ending: two rungs; api-ignition staged; the
   verify checklist carries one open box — the dormant-baton
   case, reassigned to the cockpit, open until proven live.
7. LANE VEHICLES (system piece, high) — spans D-016 → D-017 →
   #48 (erratum) → D-019 → D-020 (all retired for native
   agents + worktrees) → D-041 → D-055. Ending: exactly two
   vehicles (LOCAL default, CLOUD via liftoff) plus one named
   exclusion (teams).
8. THE PARALLEL-READY MENU (system piece, high) — spans D-016 →
   D-020. Ending: retired; survives only as the pacing law's [P]
   clause. (Teammate note: may sit better on the pacing-law
   shelf — a proposal, not a claim.)
9. FIX-ROUTING TRIAGE (system piece, high) — spans D-018 →
   D-020. Ending: absorbed to one pre-review sentence in
   parallel-lanes; the three-route ladder is record-only.
10. THE HANDS DOCTRINE (system piece, high) — spans D-045
    (verbatim into SETUP) → D-054 (Opus 5 · xhigh single pick) →
    D-055 (the Sonnet-teammate clause retired). Ending: one
    litmus at SETUP §Models & effort.
11. AGENT TEAMS (system piece, high) — spans D-020 (flag on) →
    D-045 → D-055 (the boundary + two risks) → D-056
    (capture-then-kill). Ending: boundary at HOME §Agent teams;
    the superseded-seat road stays uncovered and filed.
12. THE REVIEWER SUBAGENT (system piece, high) — spans #146 →
    D-044 (wired at ship §6) → #175 (first native registration)
    → #180 → D-054 → D-055 (model = the opus alias by billing
    verification). Ending: read-only critic on model: opus,
    advisory verdicts at THE GATE, run on essentially every
    recent bench.
13. THE DELEGATION MAIDEN FLIGHT 07-16 (event, high) — spans
    #146 · #147 · D-043. Ending: closed by #153 — route 1 works
    on ready PRs only; count:runs is an upper-bound proxy; two
    crash tests absorbed.
14. SHAKEDOWN PHASE 2, THE LANE HALF 07-17 (event, high) —
    spans #170 · #171 · #175. Ending: two unattended lanes flown
    and welded; never-share-a-file proven ENGINEERABLE at bench
    construction.
15. THE BATCH-1 LANE LAUNCH 06-12 (event, MEDIUM — the
    machinery's ending is derivable, the four tasks' is not) —
    spans D-016 → D-017 → D-019 → D-020. Ending: the machinery
    retired by D-020 (bundle-seeded sandboxes could not push;
    T3+T5 work destroyed); the tasks' fate is owed.

Shelf-boundary calls: #221/#225 leaving-ritual surfaces, the
Shakedown A/N checklists, and the 07-22 cockpit-birth half are
carried as spans only.

FOUNDER INPUT OWED (lane-system): 1. THE T3–T6 BATCH-1 ARC (the
known case, confirmed independently): the vehicle's story is
fully on record, the WORK's is not — whether T3–T6 were
re-flown, folded, or abandoned; what happened to T4 and T6
(never even recorded as failed); whether their ROADMAP boxes
reflect it. No ending invented. 2. THE REVIEWER AGENT-TYPE
REGISTRATION DEFECT: graduated 07-17, ran natively at #175 and
again 07-24; the standing IDEAS line says "confirm before
closing" — needs the founder's word to write closed or carry
open.

## Where to look

- Spec:
  [docs/record/specs/chronicle-layer.md](../record/specs/chronicle-layer.md).
- The founder's queue mandate + the six rulings: this session's
  conversation, 2026-07-27.
- The team boundary: [HOME §Agent teams](../HOME.md#agent-teams);
  the leaving duty:
  [handoff §1.2](../skills/handoff.md#12--shut-the-team-down-full-only).
- The superseded shape: the 2026-07-23 line in
  [IDEAS](../IDEAS.md), closed by this bench's decide.
