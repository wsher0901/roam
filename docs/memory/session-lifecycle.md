---
type: memory
id: session-lifecycle
updated: 2026-07-23 23:13 · birth · home PC
---

# session-lifecycle — what start, pause, and close leave behind

## Status

bench ready — 2026-07-23 23:13 · control tower · home PC. Born
bench-first from fresh `main` (`058cf40`, origin verified
main-only after the board-seat-armed micro landed). Next act:
implement the six mandate items from
[the spec](../specs/session-lifecycle.md).

## What this task is

One theme — what a session's start, pause, and close leave
behind, made deliberate: the close-lock softens to READ-ONLY
CONVERSATIONAL (record unchanged, wall softened; R4b tombstone
and seat-stamp law kept strict) · interrupted work leaves a
structured ⏸ capture block and pickup leads with "continue?" ·
the `claude/*` residue sweep mechanized in pickup (Gap A) · IDEAS
learns compaction and gets its first one (Gap D) · two queued
ideas appended · one liftoff polish. Contract:
[the spec](../specs/session-lifecycle.md).

## Pending issues

- **The one file beyond the mandate's list, pre-flagged:** the
  close-lock's wall is `.claude/hooks/user-prompt-submit.mjs`
  (exit 2 on any prompt after the flag) — doctrine-only softening
  cannot operate, so the hook is softened per the
  [#197](https://github.com/wsher0901/roam/pull/197)
  gap-in-the-mandate precedent and carries to the gate for the
  founder's word.
- Founder context on record, NOT re-flagged as owed: the cockpit
  routine box was re-saved 07-24 post-D-049 · the seat debt
  cleared this sitting — the tail repaint reflects both.
- Tower-authored, so [no-solo-approval](../LAWS.md) requires an
  external Web review before the founder's word at THE GATE. DO
  NOT MERGE.

## Left / idle

All six mandate items (see the spec's Plan): close-lock softening
· interrupt capture · the `claude/*` sweep · IDEAS compaction
rule + first compaction · the two IDEAS appends · the rung-3
polish. Then verification, the gate.

## The story

**Birth.** STEP 0 held: the board-seat-armed micro
([#210](https://github.com/wsher0901/roam/pull/210)) had landed
and pruned; origin heads = `main` only; the bench cut from
`058cf40`. Pre-birth recon settled the two design questions the
mandate left open: `scripts/check-memory.mjs` needs NO change —
it excludes TEMPLATE by name and checks the six headings as an
ordered SUBSET, so both the TEMPLATE's new block and any extra
section in a real memory pass; and the close-lock's wall is
physical (the prompt hook exits 2), which makes the hook the one
lawful gap-file (narrated under Pending issues).

## Where to look

- [the spec](../specs/session-lifecycle.md) — the contract; the
  hook gap is its own section.
- [#197](https://github.com/wsher0901/roam/pull/197) — the
  gap-in-the-mandate precedent the hook fix rides on.
- [pickup](../skills/pickup.md) · [handoff](../skills/handoff.md)
  · [liftoff](../skills/liftoff.md) ·
  [TEMPLATE](../memory/TEMPLATE.md) · [HOME](../HOME.md) ·
  [IDEAS](../IDEAS.md) — the six mandate homes.
