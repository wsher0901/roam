---
type: memory
id: roadmap-recut
updated: 2026-07-13 · task birth · home PC
---
# roadmap-recut — ROADMAP recut: plain-language V1, completion criteria, per-family vetting outputs

## Status
Bench born and claimed by the cockpit (home PC seat). Spec + this
stub are the first commits; the draft PR goes up immediately after.
No work landed yet.

## What this task is
A docs-only recut of [ROADMAP](../ROADMAP.md), fully specified in
the Web-chat workshop (slug roadmap-recut). Three moves: the file
opens by saying plainly that the plan is living and changes only by
decision; the V1 block becomes a prose goal a stranger can read
(and the "Active stages" stored-status lines die, per the derivation
law); "Done when" becomes "Completion criteria" everywhere in
ROADMAP and the living docs (the spec term "Done-means" is a
different artifact and keeps its name). Plus the
[D-020](../DECISIONS.md#d-020--2026-07--parallel-lanes-v2-native-lanes-replace-hand-built-orchestration)
alignment: T3–T6 each write their own `SOURCES-<family>.md` file so
four [P] lanes never share a file; T7 consolidates and deletes them.

## Pending issues
none

## Left / idle
Everything: the decide entry (next D-number computed from
[DECISIONS](../DECISIONS.md)' tail = D-026) + the full ripple in one
commit, then verification, then ship to THE GATE (no merge — the
founder's approval follows external review).

## The story
Born from the roadmap-recut Web-chat workshop: the founder arrived
with edits 1–8 verbatim and the mechanism pre-decided (one D-number,
entry + ripple in the same commit). Claim check found zero open
branches and zero open PRs — the cockpit takes the lane directly.
The term-rename hit list was derived at execution time as
instructed: ROADMAP's nine "Done when" occurrences (versions-table
header + eight stage lines) and three [HOME](../HOME.md) lines
(ROADMAP portrait; the version/stage/task term; the Roadmap-manual
notation bullet). One hit in [DECISIONS](../DECISIONS.md) stays as
written — append-only, excluded by the brief.

## Where to look
- spec: [../specs/roadmap-recut.md](../specs/roadmap-recut.md)
- the roadmap-recut workshop chat (Web) — where edits 1–8 were cut
- target files: [ROADMAP](../ROADMAP.md) · [HOME](../HOME.md) ·
  [DECISIONS](../DECISIONS.md)
- PR: opens as draft at birth (number lands here at first rewrite)
