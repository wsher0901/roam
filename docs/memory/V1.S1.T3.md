---
type: memory
id: V1.S1.T3
updated: 2026-09-15 · critic round answered · lane (work PC)
---

# V1.S1.T3 — vet sources: Sky & sea

## Status

airborne · local agent-team lane (work PC) · 2026-09-15

Acked by the control tower; the lane is licensed to work from this
commit. Previous Status line, kept because the flight is the record:
`claimed — local agent-team lane (work PC) · 2026-09-15 15:55 EDT.`
Canary pushed on this commit; awaiting the tower's ack commit on
origin before any real work begins.

The 2026-09-10 14:03 EDT claim above it is SUPERSEDED: that lane
never received an ack and is dead. This is a fresh claim on the same
bench, taken after merging origin/main (which carried
[D-088](../record/DECISIONS.md#d-088--product-the-september-re-tailoring--retrieval-the-optimizer-cost-trend-state)
into this branch).

## What this task is

Vet a real source for each of the 7 Sky & sea
slots, prove each with a spike fetch that runs, and write the
entries to `docs/data/SOURCES-sky-sea.md`. Contract:
[the spec](../record/specs/sky-sea.md).

## Pending issues

**One deviation, and it is an environment artifact, not a diff
defect.** `npm run build` cannot run from this worktree: the
worktree's own `node_modules/` is empty (only a `.vite` cache), so
Node resolves eslint/prettier/vitest upward from the parent clone and
they work, but Turbopack refuses to look above what it detects as the
workspace root and reports "Could not find the Next.js package". Every
other preflight command is green locally. The diff touches only
`docs/` and `scripts/spikes/`, nothing the build compiles, and CI runs
`npm ci` from a clean checkout — so
[D-038](../record/DECISIONS.md#d-038--ci-is-the-arbiter) settles it
and the pushed head's Actions run is the arbiter, exactly as the law
says it is.

## Left / idle

Nothing held. Three GAPS are named in the SOURCES file's tail rather
than parked — they are results this bench produced, and
[V1.S1.T7](../ROADMAP.md#v1s1--data-definition-the-gate-docs--spike-scripts-only-no-app-code)
consolidates them: tides outside US waters, foliage and migration
timing everywhere, and night-sky darkness everywhere today.

## The story

**2026-09-15 — the spec gains D-088 ruling 3's duty.** The ack
landed and the first act after it was NOT source work: origin/main
had moved under this bench, and
[D-088](../record/DECISIONS.md#d-088--product-the-september-re-tailoring--retrieval-the-optimizer-cost-trend-state)
ruling 3 names T3–T6 as adding their own retrieval-policy rows WHEN
THEY RESPAWN. So the merge came first, then the spec
([sky-sea](../record/specs/sky-sea.md)) gained a section fixing the
row's three fields, the three rules that bind it (`n/a` says why ·
the policy is per fact and an entry-level row declares itself a
shorthand · grade B is domain CLASS per
[ENGINE §3](../ENGINE.md#3-acquire--get-the-facts), never a
promotion by the row that lists the domain), a corrected Plan step 5
and a new Done-means checkbox. Plan step 5 had to be corrected
rather than extended: it said "LLM-research grade", a phrase
[D-088](../record/DECISIONS.md#d-088--product-the-september-re-tailoring--retrieval-the-optimizer-cost-trend-state)
ruling 1 RETIRED because it collapsed rung 5a and 5b and so hid the
better one.

**2026-09-15 — the critic round: seven findings on my own diff, all
answered.** Four mechanical checks first (files touched vs main; the
twelve entry fields present in all seven entries; every SS-01..10
claimed by exactly one slot; every named spike existing on disk with
none orphaned) — all clean, and a fact-ID anchor checker of my own
flagged two false positives that `check:links`, which implements
GitHub's real slugger, disproved. Then the substantive read, which
found seven:

1. **A claim my own evidence refutes.** The tides entry said NOAA
   CO-OPS is "US states/territories only" — and the spike's OWN
   nearest-station table showed the nearest station to Sydney is
   **Niue, which is not US territory**. Measured properly: 3,068
   stations carry a US state or territory code, **431 carry none**,
   and the stateless set includes Papeete, Christmas Island,
   Kwajalein, Majuro. Corrected in BOTH the entry and the spike to
   "the US plus a thin Pacific tail" — the conclusion (no useful
   coverage for Europe, Australia, Korea, Japan) survives, the
   characterisation did not. **This is the finding that justifies the
   round**: it was a reasonable-sounding summary of a US agency, and
   the data was sitting in my own output contradicting it.
2. **A licence I asserted instead of reading.** I wrote "US-Gov
   public domain" for the VIIRS VNL composite, inferred from
   "NOAA-funded". EOG's own product page says the data are under
   **CC BY 4.0** with a citation duty — and hedges, "**many of** the
   VIIRS Nighttime Lights data". Corrected, with the hedge quoted and
   a note that the specific annual composite must be confirmed at
   download time. Still clears
   [D-015](../record/DECISIONS.md#d-015--data-asset-law); the class
   was simply wrong.
3. **A domain class I inferred for a source that does not claim it.**
   The nature-timing retrieval policy called USA-NPN B "as a
   government-hosted national programme". Its About page claims
   federal *compliance*, not agency status. Removed: USA-NPN answers
   at RUNG 2, so
   [ENGINE §3](../ENGINE.md#3-acquire--get-the-facts)'s domain
   classes do not govern it at all, and the entry now says so instead
   of inventing an exception.
4. **A licence page applied beyond its subject.** NASA's Images and
   Media Usage Guidelines addresses images, audio, video and media
   files; I used it to clear a tabular eclipse catalogue. The entry
   now records that as an INFERENCE from the same
   US-Government-work basis rather than as a verified grant.
5. **Unsourced precision.** "654 columns documented in its own
   header" for the IAU MDC file — a number I did not read. Replaced
   with what is actually observable: a column ruler in the file's own
   header.
6. **A grade with no run, stated as if it had one.** The file's
   header claimed every grade is backed by a live run, but
   night-sky-darkness's B is a PROJECTION for an asset behind a login
   wall. Both the header and the entry now declare the exception —
   surfaced at the top as well as in place, because an exception
   buried in one entry is one that gets copied forward silently.
7. **A grade whose reasoning was missing.** astro-ephemeris grades A
   while being computed, and
   [ENGINE §7](../ENGINE.md#7-render--honest-pixels)'s C row says
   "derived". The entry now argues it — a deterministic ephemeris is
   not an estimate, and rendering sunrise as a "labelled estimate"
   would make it look LESS sure than it is — and flags the question
   for [V1.S1.T7](../ROADMAP.md#v1s1--data-definition-the-gate-docs--spike-scripts-only-no-app-code)
   (OPEN-2), since where rung 3 grades is a scale question.

**2026-09-15 — seven slots vetted, every grade backed by a live run.**
The bench's rule was that a grade with no run is an assertion, so
seven zero-dependency spikes were written and run, and each entry
records the command and what the real response returned.

The calls that were NOT obvious, and why they went the way they did:

- **astro-ephemeris is computed, not fetched, and the proof is a
  cross-check.** Rung 3 answers SS-01..03 exactly everywhere, so the
  spike implements the NOAA solar equations and a Meeus lunar model
  and compares them against the US Naval Observatory: worst sun-event
  delta 3 minutes, moon illumination exact to the percent at three
  sites. USNO is the ORACLE, never the source — and the computed path
  covers astronomical twilight, which USNO's `oneday` service does
  not return at all.
- **Two gaps were MEASURED rather than described.** "Tides are patchy
  outside the US" became: nearest NOAA station 4,847 km from
  Mont-Saint-Michel, 2,479 km from Sydney, 2,567 km from Jeju.
  "Nature timing is weak" became: 12 of 12 US Spring-Index queries
  returned a value, 0 of 8 non-US did, and a mechanical walk of all
  129 USA-NPN layer names found spring index and growing-degree-days
  present, **foliage and wildlife migration absent entirely**.
- **night-sky-darkness has no source at all, and the spike proves it
  four ways** — key wall, login wall, imagery-only, and a licence.
  The licence is the one worth keeping: the Falchi World Atlas 2015,
  the dataset a hurried vetting WOULD have picked, is **CC BY-NC**,
  quoted from its publisher's own page — a
  [D-015](../record/DECISIONS.md#d-015--data-asset-law) disqualifier.
  It is named in the entry precisely so nobody re-adopts it later on
  reputation.
- **A validation that failed the naive test was kept, not tuned
  away.** aurora-viability was checked against SWPC's OVATION
  nowcast; ranking the sites by geomagnetic latitude does NOT
  reproduce OVATION's probability ordering, because OVATION is
  magnetic-local-time dependent and Fairbanks was on the dayside. The
  honest check — nightside sites only — gives 46% inside the oval
  against 4% outside, and the limit is written into the entry:
  OVATION validates the gate's SHAPE and can never validate a
  season-scale verdict.
- **Two grades were SPLIT because one grade would have lied.**
  aurora-viability is B for the darkness gate (exact) and C for the
  Kp-threshold verdict (a curated rule of thumb). astro-events is A
  for eclipses (a published canon) and B for shower peaks (the IAU
  catalogue holds several published solutions per shower — the eta
  Aquariids span 12.6° of solar longitude, about twelve days, so the
  spread has to render).
- **Three retrieval policies are real rows; four slots write `n/a`
  with a reason.** The `n/a`s are not silence: astro-ephemeris,
  aurora-forecast, aurora-viability and astro-events each say WHY no
  rung-5a path exists, and SS-10 writes its own `n/a` separately from
  SS-04 because the two facts differ — so the tides row is split and
  names its fact IDs rather than shorthanding the entry.
- **Where a B tag needed justifying, it is justified in the entry.**
  DarkSky International is tagged B **only** for whether a place
  holds its own certification (it operates that programme), and C for
  any sky-brightness number it publishes. A university phenology
  group is tagged C: good science is not one of
  [ENGINE §3](../ENGINE.md#3-acquire--get-the-facts)'s domain
  classes, and grading it B would be the promotion the rule forbids.

**A dead end worth recording:** NOAA's geomag-web calculator API, the
obvious route to magnetic inclination per point, is now KEY-GATED —
it returned HTTP 400 with "Either the key parameter is missing or it
is wrong". The route taken instead is better anyway: fetch the IGRF-14
Gauss coefficients from IAGA/NOAA and derive the centred dipole pole
locally, which lands at 80.83°N 72.80°W — matching the published pole
position, and with no key, no quota and no typed constant.

Born 2026-09-10. This bench is lane-disjoint from its three siblings
by construction: it writes only its own `SOURCES-<family>.md` and
its own `scripts/spikes/sky-sea-*` files, and never
[SOURCES.md](../data/SOURCES.md), which
[V1.S1.T7](../ROADMAP.md#v1s1--data-definition-the-gate-docs--spike-scripts-only-no-app-code)
consolidates.

## Where to look

- [the spec](../record/specs/sky-sea.md) — the contract
- [FACTS SS-01..10](../data/FACTS.md#f-ss--sky--sea-10--source-task-v1s1t3) — the facts served
- [SOURCES.md](../data/SOURCES.md) — the format, as shipped for Weather
- the draft PR — the public window
