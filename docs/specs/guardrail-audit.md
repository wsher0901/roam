---
type: spec
id: guardrail-audit
title: Guardrail audit — the harness at best practice; two promotions and the proof on record (D-053)
status: open
stage: workshop
branch: docs/guardrail-audit
pr: 218
opened: 2026-07-24
shipped: —
---

# guardrail-audit — already compliant; ship the two real improvements

**Links:**
[LAWS](../LAWS.md) · [HOME](../HOME.md) ·
[SETUP §cloud accounts](../SETUP.md#once-and-done--cloud-accounts)
· [DECISIONS](../DECISIONS.md) · [IDEAS](../IDEAS.md)

## Goal

The founder's maximum-depth audit of the whole governing layer
(LAWS · HOME · SETUP charter · all nine stubs · all three hooks ·
the reviewer agent · settings · AGENTS) against Anthropic's
guidance found the harness ALREADY AT BEST PRACTICE. This bench
ships the audit's only two real improvements and records the
proof as D-053 — the audit AS the record.

## Out of scope

- Every rejected alternative (D-053 names each with its reason):
  a separate BRIEF.md / `.claude/rules/` · a marker-economy pass ·
  duplicating HOME's routing table into LAWS · a session-shape
  law line · settings/hook changes.
- Any stub beyond liftoff and ship — the audit read all nine and
  the rest are precise (recall the house exemplar).
- The decide-anchor shortening pattern — an IDEAS line, not now.

## Plan

1. **LAWS §Workflow — the verification loop promoted**: the
   CI-mirror sentence inside the "Before saying done" bullet
   becomes a labeled, ordered, copy-runnable block (the bullet's
   sync-first and Actions-green clauses kept around it).
   Wording-only; zero semantic change.
2. **Two stub descriptions sharpened, one line each**: liftoff
   gains the condition + boundary (machine going dark;
   watch-from-phone belongs to go-remote, never liftoff); ship
   gains the moment (payload complete + CI mirror green, BEFORE
   any "done" claim in conversation).
3. **DECIDE — D-053**, house style, the audit as the record;
   the rejected list with reasons; one IDEAS line (the
   ~300-char decide-anchor links in LAWS — a future
   consideration).

## Files touched — nothing outside

[LAWS](../LAWS.md) · `.claude/skills/liftoff/SKILL.md` ·
`.claude/skills/ship/SKILL.md` · [DECISIONS](../DECISIONS.md) ·
[IDEAS](../IDEAS.md) · this spec +
[the memory](../memory/guardrail-audit.md) ·
[DASHBOARD](../DASHBOARD.md) via the ship tail.

## Done means

- [x] The verification block present in LAWS, labeled, ordered,
      copy-runnable; the sync-first and Actions-green clauses
      intact around it; zero semantic change.
- [x] Both stub descriptions carry their new clause; grep
      confirms no other stub changed.
- [x] D-053 in DECISIONS, house style, the audit's findings and
      the rejected list with reasons; the IDEAS line appended.
- [x] `check:links` · `check:ledger` · `check:memory` green. (2026-07-24, home PC, on the completion tree.)
- [ ] Full CI mirror green locally AND the pushed head's Actions
      run green.
- [ ] [ship §6](../skills/ship.md) critic runs; verdict rides to
      the founder verbatim.

## Pending

Manual acts after the weld: NONE — the charter is untouched; no
box re-save. The queue: THE CHRONICLE LAYER (its own chat).

## Deviations

none — the three items landed exactly as mandated; the diff-stat proves the two stubs are the only .claude/skills/ changes.

## Open questions

None at birth. This bench ships to the gate and does NOT merge.
